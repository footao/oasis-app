# おあしすっち レース予想ツール — 引き継ぎ資料

このファイルは、本プロジェクトを別のAI／開発者が引き継ぐための総合ドキュメントです。
（作成時点のコア版: **CORE_VERSION 3.6.1** / 対象ゲーム: Discordボット「おあしすっち」のレース）

---

## 0. まず読むべきこと（TL;DR）

- **これは何か**: Discord上の育成レースゲーム「おあしすっち」で、各馬のステータスとパッシブから
  着順を予測し、**3連単**と**単勝**の期待値（EV）ベースの買い目を推奨するツール。
- **構成**: ロジック核 `oasis_core.py`（UI非依存, ~2770行）＋ Streamlit UI `oasis_app.py`（~930行）。
  Streamlit Community Cloud（GitHub: `footao/oasis-app`）にデプロイ。学習ログはGitHubの `logg/` を既定参照。
- **データ取得**: **`api.oasis.red` にはサーバからも到達できる**（2026/08/14 実測で訂正。
  旧記述の「403で到達不可」は誤り）。**結果APIは token 不要**で guild と user だけで叩ける。
  購入系だけがブラウザ（BOT発行リンクの token）を要する。`harvest_results.py` で
  サーバから直接採取できる。
- **モデル**: レース内で中心化した log(score) を的にした Ridge回帰（距離別の log項＋線形項の
  ハイブリッド特徴量）＋ モンテカルロで着順分布 → パリミュチュエルEV。
- **最重要の設計哲学**: σ（着順のブレ幅）は**手で膨らませない**。過去に「安全のためσ×1.25」を入れて
  逆効果（偽の+EV量産）だったため撤回済み。資金保全は分数ケリー・モデル信頼度λ・最小的中率フィルタが担う。
- **作業フォルダ**: `C:\oasissfable\repo`（= GitHub リポジトリのクローン、2026/08/15〜）。
  ここで直接編集し `git add -A` → `commit` → `push`。以前は `oasis-app-v2_6` で作業して
  robocopy でコピーしていたが、二重管理をやめた。**`oasis-app-v2_6` は過去の残骸なので触らないこと。**
  app と core は**必ずセット**で更新（起動時に版チェックで不一致を止める）。
  `races.jsonl`（採取した生データ・gitignore対象）も作業フォルダに置く。
- **⚠ 同じファイルが3箇所にある（未整理）**: `model.js` などが
  `bookmarklets/src/`（**これが本物**）/ `src/`（残骸）/ リポジトリ直下（残骸）に重複している。
  `build_autopilot.py` が読むのは `bookmarklets/src/` だけ。他を編集しても何も起きない。

---

## 1. ファイル構成（`repo/` = 配布物一式）

| ファイル | 役割 |
|---|---|
| `oasis_core.py` | ロジック核。学習・予測・シミュレーション・EV・ログ解析・パッシブ計算。UI非依存。`CORE_VERSION` を持つ。 |
| `oasis_app.py` | Streamlit UI。3タブ（🎯予測／🔬モデルを見る／📒実績ログ）。起動時に `REQUIRED_CORE` と core の版一致を検査し、不一致なら明示メッセージで停止。 |
| `passive_spec.json` | 35種パッシブの数値（倍率・スコープ等）。学習で補完した値も貯める。 |
| `logg/` | 学習用のDiscordエクスポート（レース情報・アップデート情報）。既定の学習データ源。実ユーザー名（@ハンドル）を含む。**リポジトリは public、実名のまま公開で確定**（2026/08/15 オーナー判断）。匿名化したくなったら `anonymize_logg.py` を実行する（`@ぷち`→`@u001`、同じ人は同じ番号。特徴量の差 0.0 で精度は落ちないことを実測確認済み。ただし過去のコミットに実名は残る）。 |
| `anonymize_logg.py` | `logg/` の @ハンドルを連番へ置換。出走表(`👤 @名`)と結果(`@名`)の**両方**を置換しないと、出走表↔結果の突き合わせが外れてコンディションが取れなくなる。`--selfcheck` / `--dry` あり。 |
| `sheets_backend.py` | ベットログをGoogleスプレッドシートへ保存（任意）。書き込みは update→末尾clear の順（失敗時のwipe防止）。 |
| `drive_backend.py` | 学習ログをGoogleドライブから取得（任意・ページング対応）。 |
| `oasis_bookmarklet_v2.html` | オッズ・出走馬・パッシブ効果の取得ブックマークレット（bm.js）。 |
| `oasis_buy_bookmarklet_v2.html` | 買い目の一括購入（buy.js）。 |
| `oasis_probe_bookmarklet.html` | 単勝プールの試し買い実測＋全データ取得（probe.js）。 |
| `oasis_harvest_bookmarklet.html` | 過去の確定レースを result API から一括採取し、loggと同書式で出力（harvest.js）。学習データ増やし用。 |
| `bookmarklets/src/*.js` + `minify.py` | ブックマークレットの元ソースとビルダー。`minify.py` が `//` コメントを安全に落として1行化しHTMLへ注入。 |
| `make_formula_doc.py` | 現在の学習係数から「スコア計算式」閲覧用HTML(`oasis_score_formula.html`)を生成。 |
| `oasis_score_formula.html` | 上の生成物（内部式・モデル式・パッシブ係数の表）。 |
| `selftest.py` | ログ解析→学習→予測→ベットログ→**回帰テスト15件**の一気通貫。`python3 selftest.py logg`。 |
| `README.md` / `DEPLOY.md` / `REVIEW.md` | 使い方 / デプロイ手順 / 設計レビュー（重要な判断の記録）。 |
| `requirements.txt` / `oasis.spec` | 依存 / PyInstaller用スペック。 |

> 注: セッション作業ディレクトリには `e2e.py`（AppTest含む一気通貫）や `verify.js`
> （ブックマークレット構文チェック, Node）などの検証スクリプトもある。

---

## 2. 対象ゲームとデータ

- **おあしすっち**: 各馬に スピード / パワー / スタミナ の3ステータス、コンディション（好調/普通/不調）、
  パッシブスキル（**2枠まで**装備、35種）、成体種（adult_key, 同族判定用）がある。
  距離（短距離/マイル/中距離/長距離）×馬場（芝/ダート）でレース。
- **賭け式**: 3連単（1着2着3着を順番通り, **8頭以上**で成立, 合計**20口**まで, 1口10,000rrc,
  キャリーオーバーあり）と 単勝（合計**100口**まで, 1口**1,000rrc**, 純パリミュチュエル控除ありだが
  プール総額はAPIで取れず要実測）。
- **データ源**:
  - `logg/` のDiscordエクスポート（`🏁 …結果` ブロックから 着順・score・ステータス・パッシブを解析）。
  - result API（`/api/race/result/{guild}/{date}/{race}`）と by-id API
    （`/api/race/by-id/{guild}/{schedule_id}`）。**サーバから到達可・token不要**。
    保持期間は少なくとも15日（2026/08/14時点で 07/30 のレースがtimeline付きで取得できた）。
    result の各エントリには **`timeline`**（100mごとの残スタミナ・消費・疲労補正）と
    `stamina_after` `finish_time` がある。
- **スコア式の変更**: 2026/07/28 に着順スコアが「タイム基準」に変更（`SCORING_PATCH_DATE`）。
  学習は既定でこの日以降のみ（`DEFAULT_TRAIN_FROM`）。距離ごとに絶対値スケールが激変するため、
  **ターゲットはレース内で中心化した log(score)**（スケール不変）。
- **乱数幅の変更**: 2026/08 のプチ修正で、レース中の実効ステータス乱数幅が **±1.5% → ±3%**
  （`STAT_RNG_WIDTH`）。荒れやすくなり、順番勝負の3連単に強く効く（後述の3連単専用σと VARIANCE_SHARE）。

---

## 3. モデリング設計

1. **ターゲット**: `y = center_by_race(log(score))`。距離ごとのスケール変化やゲーム側リスケールに強い。
2. **特徴量（ハイブリッド, v3.0〜）**: 距離ごとに `切片 + b_log·log(実効stat) + b_lin·(実効stat/100)`。
   log項は比率で効く頑健な土台、線形項は**内部式の加法構造**（特に長距離のスタミナ）を捉える。
   実効statにはスペック済みパッシブの倍率が畳み込まれている。未スペックのパッシブはダミー＋距離交互作用
   （`INTERACTION_SHRINK=0.5`）で学習。状態（好調/不調）も係数。
3. **学習器**: Ridge。αはレース単位クロスバリデーションで選択（順位スピアマンを最優先）。時間減衰重み
   （half_life 21日, blendedモード時）。
4. **σ校正**: OOF予測に対し「実際の上位 top_k 着順の尤度」を最大化するσを探索（`_calibrate_sigma`）。
   - **単勝用 `race_sigma`**: 全頭数レース・**`top_k=1`**（1着が誰かだけ当てればよい）。
   - **3連単用 `tri_sigma`（v3.2.0〜）**: **8頭以上**のレース・**`top_k=3`**（順番まで当てる）。
   - **⚠ v3.6.1 で2つのバグを修正**（2026/08/15）:
     1. 単勝用も `top_k=3` で校正していた。3連単用の尺度で単勝のσを決めていたことになり、
        σが大きく出て本命の勝率を過小評価していた（実測で 予測72% vs 実測94%）。
     2. 探索グリッドの下限が `0.15 × 残差std` だったため、モデルが強いときに必要な
        小さいσ（0.005前後）へ**構造的に到達できなかった**。粗い対数グリッド→周辺を細探索
        の2段階に変更し、下限を 0.002 まで下げた（評価点数は減ったので速度も改善）。

     修正後: 単勝σ 0.0182 → 0.0050、校正 予測91% vs 実測92%。
     `tri_sigma`（0.0128）> `race_sigma`（0.0050）という設計意図どおりの大小関係に戻った。
     レース数を70〜103で振っても単勝σは 0.0046〜0.0078 に収まる（旧実装は7レース増で
     0.0113→0.0182 と跳ねていた）。
5. **モンテカルロ**: `simulate_trifecta`（N_SIM=400,000）。単勝勝率は `race_sigma`、3連単の組確率は
   `tri_sigma` で**別々にシミュレーション**（`analyze` 内）。安定感など分散低減スキルは σ を下げる
   （`VARIANCE_SHARE=0.8` の割合にだけ効かせる）。
6. **EV / 買い目**:
   - 3連単はパリミュチュエル実効オッズ = (P + x) / (P_i + x)。キャリーオーバー考慮。
   - **λ混合（model_weight=0.7）**: p_bet = λ·model + (1−λ)·market。モデルと市場の食い違いの一部は
     必ずモデル誤差なので、全信頼しない。
   - **最小的中率フィルタ（min_prob=0.003）**: MCノイズ由来の偽+EV（大穴）を除外。
   - 分数ケリー（kelly_fraction=0.25）＋リスク上限（max_risk_frac=0.10）で口数配分。上限口数を厳守。
7. **単勝プール実測**: 単勝はΣ(1/最終オッズ)=1.000 の純パリミュチュエルで、**オッズはシェアしか表さず
   プール規模の情報を含まない**。少額の試し買い前後のオッズ変化から総額を逆算（重み od² の加重平均で
   丸め誤差最小）。**プール総額は1,000rrc単位**（`WIN_POOL_QUANTUM`）なので、推定が十分精密なら最も近い
   格子へスナップして「確定」させる（`estimate_win_pool` / probe.js 両方）。

---

## 3-b. スタミナの実機構（2026/08/14・result API の timeline から確定）

**CORE_VERSION 3.6.0 でモデルに反映済み**（特徴量 `スタミナ余り` / `スタミナ不足` の2列）。
定数は `oasis_core.STAMINA_COST_LAW`、計算は `stamina_budget()`。
**必ず2列セット**で使うこと（理由は下の「検証結果」）。

```
初期スタミナ = floor(実効スタミナ)            ← パッシブ倍率は効く
消費/100m    = clamp(c[距離] × Σ(序盤重み × 距離バランス × 実効ステータス), 下限, 上限)
必要スタミナ = 消費 × 区間数
不足         = max(0, 必要スタミナ − 初期スタミナ)
```

実測した定数（Discordログのステータス × API の timeline で突き合わせた 999頭）。
消費は区間ごとに微妙に違うので **cs[0]×区間数 ではなく実測を合計**して求めている:

| 距離 | c | 下限 | 上限 | 区間 | 必要ST |
|---|---|---|---|---|---|
| 短距離 | 0.01335 | 1.955 | 2.967 | 10 | 20〜30 |
| マイル | 0.01976 | 2.234 | 3.067 | 15 | 34〜46 |
| 中距離 | 0.02999 | 2.541 | 3.737 | 20 | 51〜75 |
| 長距離 | 0.04077 | 2.57 | 3.68 | 25 | 64〜92 |

早見表は `stamina_table.pdf`（`make_stamina_table.py` で再生成）。
**短距離は287頭中1頭しか不足しない**＝短距離馬にスタミナを振るのは無駄。

c が距離ごとに違うのは `INTERNAL_DIST_BALANCE` の絶対倍率のズレを吸収しているため。
レース内順位には影響しない（同一レースの全馬が同じ距離）ので問題ない。

マイル(1500m/15区間)は **必要スタミナ 34.0〜46.0**。下限に49頭・上限に67頭が
ぴったり張り付いており、クランプは確定。短距離・中距離の下限は観測がほぼ1頭で未確定。

**重要な含意**

1. **速いほど消費が増える**。レートが上限に当たると消費も上限に張り付く。
   実例: おいら（レート207.9で最高）は消費が上限3.067→46必要、持ち43で−3.0。
   **自分の速さでスタミナ切れを起こして3着**に落ちた。
2. **疲労補正は「収支」の関数**（2026/08/15 に特定）。収支 ＝ 持ちスタミナ − 必要スタミナ。
   timeline 21,040点で相関 **+0.762**（残スタミナ単体の +0.547 より強い）。

   | 収支 | 疲労補正（平均） | レース中の最低値 |
   |---|---|---|
   | −10以下 | 0.840 | 0.650（下限に張り付き） |
   | −5 | 0.941 | 0.650 |
   | −3 | 0.979 | 0.760 |
   | −1 | 0.999 | 0.964 |
   | ±0 | 1.002 | 1.001 |
   | +5 | 1.007 | 1.002 |
   | +15 | 1.023 | 1.014 |
   | +25以上 | 1.027 | 1.018（上限 1.030） |

   **⚠ 旧記述の「必要量を超えたスタミナは完全に無駄」は誤り。** 余りは +3% まで
   速度ボーナスになる（1.030 で頭打ち）。ただし**不足側の落ち方の方がはるかに急**で、
   −3 で −2%、−5 で −6%、それ以下は下限 0.650 に張り付く。
   この非対称な折れ線が、`スタミナ余り` と `スタミナ不足` を**2列に分けないと**
   表現できない理由そのもの。

**検証結果（2026/08/14）— 「余り」と「不足」は必ずセットで**

学習に使う `logg/`（Discordログ・103レース、3着セットは8頭以上88レース）で、
レース単位CVの分割乱数を8通り変えて測った結果:

| 特徴量 | スピアマン | 1着的中 | 3着セット的中 |
|---|---|---|---|
| 現行のみ | **0.9455** | 88.7% | 60.7% |
| ＋不足だけ | 0.9330 | 90.0%（4/8） | 66.9%（7/8） |
| ＋余りだけ | 0.9362 | 82.5%（0/8） | 58.2%（1/8） |
| **＋余り＋不足** | 0.9397 | **93.3%（8/8）** | **70.5%（8/8）** |

**片方だけだと不安定、両方入れると8分割すべてで改善する。**
「必要量のところでスタミナの価値が折れる」形（ヒンジ）は2列ないと表現できないため。

レース内スピアマンは −0.006 と僅かに落ちる。これは下位馬の並びが少し崩れる代わりに
**上位3頭が当たるようになった**ということで、3連単ツールとしては正しい取引。
α を3着セットで選ぶと差はさらに開く（1着 +7.3pt / 3着セット +11.6pt）。

実ログでの学習結果も改善した:

```
              3.5.0            3.6.0
1着的中        88-89%     →     94%
3連単の校正   予測31% vs 実測44%  →  予測48% vs 実測48%
```

**3連単の校正が合ったのが一番大きい。** 3.5.0 は本命1点の的中率を過小に見積もって
おり、EV計算が系統的にずれていた。

**⚠ 検証は必ず `logg/` で行うこと。`races.jsonl` で測ると答えが変わる。**
実際 2026/08/14 に、races.jsonl で「不足だけを足すと 8/8 改善（+0.0065）」という
結果を得て一度実装し、logg で測り直したら 8/8 悪化していたので撤回した。
原因は次の落とし穴: `harvest_results.py` が叩く `race/by-id` は
**そのペットの「今」のステータスを返す**。過去のレースを採取すると、
レース当時ではなく現在の（育って上がった）ステータスが付く。
実際、races.jsonl の予測精度はレースが古いほど下がる:

```
2026-07-28 スピアマン 0.660 … 08-09 0.938 … 08-14 0.957
```

**races.jsonl はステータスの学習・検証に使ってはいけない。**
使えるのは `timeline`（消費・残スタミナ・レート）と着順・スコアだけ。
ステータスが要るなら `logg/` を使うこと。上の消費法則の定数も、
ログのステータス × API の timeline で突き合わせて出したもの。

**ただし「レース当日に採った行」は例外**（2026/08/15〜）。`by-id` は「今」の値を返すので、
開催日と採取日が同じなら当時の値そのもの。実測の一致率は 当日94% / 1日後78% / 2日後67% /
2週間後49%。そこで:

- `harvest_results.py` は各行に **`harvested_at`** を書く。`--forward` で前回の続きから採れる。
- `harvest_daily.bat` ＋ Windows タスクスケジューラで**各レース開始の5分後**に自動採取（1日6回）。
- **`harvest_results.load_races(path, need_stats=True)` を通すこと。**
  `harvested_at != race_date` の行（および `harvested_at` が無い旧データ）を落とす。
  `fit_ridge_stamina.py` / `evaluate.py` / `fit_stamina.py` / `fit_formula.py` は対応済み。
  `stamina_report.py` は timeline しか使わない（ステータスは logg 側）ので除外しない。
- 既存の300レースは `harvested_at` が無いので**ステータス用途では全部落ちる**。これは正しい挙動。
  当日採取が貯まるまで、ステータス系の再検証はできない。

`evaluate.py` / `fit_stamina.py` / `fit_formula.py` は races.jsonl の
ステータスを使っているので、**それらの「実測重み」の結論も同様に信用できない**
（実際 B=実測重みは2回とも現行定数より悪かった）。

---

## 4. 逆解析したゲーム内部の着順式（参考・表示用）

result API のタイムライン等から復元。**モデルはこれを丸写しにせず参考にとどめる**（リスケール・未知の
相互作用に強くするため）。定数は `INTERNAL_PHASE_WEIGHTS` / `INTERNAL_DIST_BALANCE` に保持。

```
rating = 定数 × Σ_区間 Σ_stat( 区間重み[区間][stat] × 距離バランス[距離][stat] × 実効ステータス±乱数 ) × 疲労補正
```

- 区間重み [SP,PW,ST]: 序盤 [0.60,0.30,0.10] / 中盤 [0.45,0.20,0.35] / 終盤 [0.35,0.35,0.30]
- 距離バランス [SP,PW,ST]: 短距離 [1.4,0.8,0.5] / マイル [1,1,1] / 中距離 [0.9,1.3,1.3] / 長距離 [0.6,1.0,1.4]
- 乱数幅 ±3%（旧 ±1.5%）、疲労補正は1.0近辺。
- `internal_stat_weights(dist)` が「区間重み×距離バランス合算・SP=1正規化」を返す（表示用）。

---

## 5. ブックマークレット（4種, ブラウザで実行）

`bookmarklets/src/*.js` を編集 → `minify.py` でHTMLへ注入 → `verify.js`（Node）で構文確認。

| ソース | 役割 | rrc消費 |
|---|---|---|
| `bm.js` | オッズ・出走馬・パッシブ効果を取得しツールへ貼れる形でコピー | なし |
| `probe.js` | 単勝を1口ずつ試し買いしてプールを実測（1000rrc単位にスナップ, 確定で打切り）＋全データ取得 | あり |
| `buy.js` | 推奨買い目の一括購入（3連単・単勝, 上限口数厳守） | あり |
| `harvest.js` | 過去の確定レースを result API から一括採取し **loggと同書式**で出力（学習データ増やし） | なし |

- パッシブ名はページ内辞書 `PASSIVE_INFO`（コード→名前）を優先。ゲーム追加スキルに自動追従。
- 「締切」判定は `phase`（=betting なら受付中）で見る。`locked`（=出走確定）は締切ではないので使わない。

---

## 6. デプロイと版管理

- GitHub `footao/oasis-app` に一式を置き、Streamlit Community Cloud が参照。
- 既定の学習データ源は「リポジトリ内 `logg/`」。Google Drive / アップロードも選択可。
- **版チェック**: `oasis_app.py` の `REQUIRED_CORE` と `oasis_core.py` の `CORE_VERSION` が一致しないと
  起動時に日本語で停止（片方だけ更新した時の意味不明な AttributeError を防ぐため）。機能追加時は両方を上げる。
- 更新手順: zipの中身を**まとめて**アップロード（app と core はセット）。反映されない時は Reboot app。

---

## 7. 変更履歴（目的つき）

> 版番号は `CORE_VERSION`。日付は概ねの作業順。

- **初期リライト（v2.7系）**: 2026/07/27 の大型アプデ（新スコア式・パッシブ2枠・35種）に合わせて全面刷新。
  旧「残差std直用」から σ自動校正へ。目的: 新ゲーム仕様への追従。
- **パッシブ仕様の取り込み**: 公式表記・購入画面 `buy.html`・スプレッドシート画像から35種の倍率を抽出し
  `passive_spec.json` 化。友達の見解は無視しデータのみ採用。目的: 実効ステータス計算の確定化。
- **ブックマークレット刷新**: 2枠対応・効果取得・成体種・自分の購入額を出力。目的: 予測に使えるデータ整備。
- **Streamlit移行＋GitHub参照**: 旧版を上書きデプロイ。`logg/` をGitHubから参照する既定化。
- **単勝プール実測 + 1発ブックマークレット**: 純パリミュチュエルでプール規模が取れない問題に、試し買い
  前後のオッズ差から逆算する仕組み。probe.js で「取得→試し買い→再取得」を1クリック化。上限（単勝100口/
  3連単20口）を厳守。
- **バグ23件修正**（サブエージェントレビュー）: 「10着+」のランク破損（正規表現）、同名馬の確率コピー、
  単勝記録の10倍額、プール二重計上、読み込み失敗時のログwipe、購入二重送信、口数上限未適用 等。
- **根本レビューでσ安全係数を撤回**: 「σ×1.25で弱気＝安全」は誤り。σ膨張はロングショット過大評価＝
  偽+EV量産（シミュで87→666件）。既定 `SIGMA_SAFETY=1.0` に戻し、保険をλ混合＋min_probへ移管。
- **v3.0 内部式のハイブリッド反映**: result API から内部式を復元し、log項に**線形の実効stat項**を追加。
  hold-outスピアマン 0.898→0.926。長距離のスタミナ過小評価を是正。`パッシブの効き目`・`内訳`表示も対応。
- **v3.1.0 スコア計算式の表示 + 単勝プールの1000rrcスナップ**: 「モデルを見る」タブと単体HTML資料に
  内部式・モデル係数・パッシブ係数表を表示（`model_formula` / `passive_coef_table` / `internal_stat_weights`）。
  単勝プールは1000rrc単位なので推定を格子スナップし「確定」表示（十分精密な時）。
- **v3.1.1 乱数幅 1.5%→3% 対応**: `VARIANCE_SHARE` 0.5→**0.8**（ゲームRNGが総ブレの主成分に。安定感等の
  価値が正しく上がる）。σは手動調整せず自動校正に委ねる方針を明記。
- **v3.1.2 乱数幅を資料/画面に明記**: `STAT_RNG_WIDTH=0.03`（旧0.015）を定数化し、内部式の説明に反映。
- **（データ更新）**: レース情報ログを新エクスポートに差し替え。新式レース 34→74。スピアマン0.945→0.950。
- **v3.2.0 3連単専用σ + 買い方ガイド**: 「1着は当たるが3連単が当たらない」を解析。8頭以上の実レースでは
  現状σが**3連単に過信**（本命1点 予測44%/実測37%）と判明（組み込み校正が良好に見えたのは8頭未満の
  易しいレース混入が原因）。**8頭以上・順番的中で校正した `tri_sigma`（≈0.020）を分離**して3連単MCに使用
  （単勝は `race_sigma` のまま）。校正が予測35%/実測40%に改善。予測タブに「買い方ガイド（狙う的中率別の
  必要点数・軸1頭流し）」、モデルタブにσ2本立て表示を追加。
- **v3.3.0 データ層の重複・キー衝突修正 ＋ オッズ下限の誤判定修正**（厳格レビュー由来）:
  - **race_key にレース番号を追加**（`_race_key`）。従来は「日付＋時刻」だけだったため、同日同時刻の
    別レースが**1レースに合成**されていた（実測: 4頭立て2レース → 8頭立て1レースに化ける）。
    `harvest.js` も `🏁 第{schedule_id}レース 結果` を出すように変更（`race_time` が取れず `0:00` に
    なるレースが全部1つに潰れるのを防ぐ）。
  - **レース単位の重複除去を「中身の指紋」方式に変更**。旧実装は除去キーに `owner` を含めており、
    harvest ログが owner を常に `@Unknown` で出すため**除去が一切効かず**、Discordログと併用すると
    同一レースが2重に入っていた（実測: 8行 → 16行、`n_field` が 4→8 に倍増）。
    距離・馬場＋(着順, SP, ST, PW) の並びを指紋にして、ソースをまたいだ同一レースを1件に集約する。
  - **オッズ 1.5 の意味の曖昧さを解消**（`diagnose_floor_odds`）。1.5 は未投票馬のプレースホルダで
    あると同時に**ゲームの最低オッズ**でもあり、シェア2/3超の大本命は実際にお金が入っていても 1.50 と
    表示される。これを「投入額0」と誤認すると実効オッズを桁違いに過大評価する
    （実測: シェア70%の本命に**実効od 201倍・エッジ+14372%** の買い推奨が出ていた）。
    純パリミュチュエルの `Σ(1/od)=1` を使い、下限表示馬を除いた合計の不足分から判定する。
    1頭だけなら本当のオッズ（1/シェア）に補正、複数で判別不能なら推奨から除外して警告。
  - **`analyze` の設定マージ修正**: `None` が既定値を上書きしてしまい `model_weight=None` で
    `TypeError` になる問題（`if v is not None` のみに変更）。
  - **`selftest.py` に回帰テスト10件を追加**（`regression_tests()`）。上記の各バグを機械的に再検出できる。
- **v3.4.0 中程度の指摘の是正 ＋ 貼り付けボタン**:
  - **M3 プールAPIの待ち時間**: `ENABLE_POOL_API=False` を既定に（`POOL_API_TIMEOUT=2.0`）。
    サーバから `api.oasis.red` へは到達できず、Streamlit は処理が直列なので待ち時間＝操作不能時間。
    ブックマークレットは常に `pool=` を出すので通常運用では不要な経路。到達できる環境なら True に。
  - **M4 精算の払戻をオッズ実績で計算できるように**: `BetLog.settle(..., final_odds={'3連単':.., '単勝':..})`。
    パリミュチュエルはオッズが締切まで動くため、購入時オッズのままだと払戻・ROIが系統的にずれる。
    ログに `payout_kind`（実績/概算）列を追加し、レポートに「概算が何件混じっているか」を表示。
    旧ログ（列なし）も `_normalize` が自動補完するのでそのまま読める。
  - **M5 `st.stop()` の巻き添え**: 単勝の重複記録時に画面全体が停止し、精算・レポート・ログ一覧が
    丸ごと消えていた。3連単側と同じ `else` 構造に統一。
  - **貼り付け欄の操作ボタン**: 「📋 クリアして貼り付け」（クリップボードから1クリックで置換）と
    「🗑 クリア」を追加。前者は Streamlit に該当APIが無いため親DOMへ実ボタンを注入して実現し、
    クリップボード読み取りを拒否されるブラウザ（Firefox 等）では Ctrl+V を案内して素通しする。
    入力欄は `key="race_input"` 方式へ変更。埋め込みは廃止予定の `st.components.v1.html` ではなく
    `st.iframe` を優先（フォールバックあり）。
  - 回帰テストを 10 → 15 件に拡張（M3/M4 を追加）。
- **v3.5.0 M2 と軽微項目の一掃**:
  - **M2 モンテカルロの二重実行**: `tri_sigma == race_sigma` のとき、単勝用と3連単用のMCが
    シードまで同一＝完全に同じ結果を2回計算していた。同σなら1回で両方受け取るように変更。
    σが異なる場合も、単勝側は `need_combo=False` で3連単の組集計を省き、1着の判定を
    `argsort`→`argmax` に落とした（16頭×40万試行で効く）。**結果は完全に同一**（テスト済み）。
  - **軽微1 `bare()`**: `' #1'`（ツール由来）しか外せず、`'#1'`（ゲーム／ブックマークレット由来）に
    対して素名フォールバックが効いていなかった。両方に対応（`_DUP_RE`）。同名馬がいる場合に
    フォールバックしない安全装置（`_bare_unique`）はそのまま機能することを確認済み。
  - **軽微3** `_train_cached` の docstring 二重定義を統合。
  - **軽微5** 「↩ 直近レースを取消」は精算済みの行も消えることをUIに明示。
  - **軽微6 `minify.py`**: マーカーが見つからないと `re.sub` が黙って何もせず、
    **ビルド成功に見えて中身が古いまま**になりえた。`re.subn` で置換件数を検証し、
    0件なら `SystemExit` で止める。
  - **軽微7 `harvest.js`**: 「連続欠損で打ち切り」がコメントだけで未実装だった。
    欠番が18件続いたら打ち切るよう実装（無駄なAPI呼び出しを避ける）。ヘッダの採取範囲も実測値に。
  - **軽微8 `probe.js`**: レース再取得に失敗すると `my_amount` が試し買い前のままで、
    自分の投入額から `spent` を取りこぼしていた。`max(実測, own+spent)` で下限を保証。
  - **軽微9** `mc_note` が単勝σしか出していなかったので、3連単σも併記。
  - **軽微10** `logg/` に実ユーザー名が含まれる点を本書に明記（学習に owner は未使用）。

---

## 8. 重要な設計判断と根拠（再掲・必読）

1. **σを手で膨らませない**。真のσより大きくすると分布が平坦化しロングショットを過大評価→偽+EV。
   保険は「分数ケリー・λ混合・min_prob」。（`SIGMA_SAFETY=1.0` を維持）
2. **ただしσは"正しい値"に合わせる**のは別問題。3連単は順番に敏感で真のσが単勝より大きいため、
   `tri_sigma` を分離した（これは膨張ではなく校正の是正）。
3. **内部式は丸写ししない**。ハイブリッドで市場・パッシブ・状態も同時学習し、リスケールや未知の
   相互作用に耐える。
4. **公開情報が多い＝単純計算で足りる、わけではない**（ユーザーの問い）。乱数・疲労・相互作用があるため
   回帰は依然有効。ただしパッシブ倍率など既知の構造は特徴量に畳み込み、学習に頼りすぎない。
5. **プールは1000rrc単位**の事前知識で推定を確定させる（小プールで特に有効）。

---

## 9. 主要な定数と関数（コードの入口）

**定数（`oasis_core.py` 冒頭付近）**
`MAX_TOTAL_UNITS=20`（3連単合計）, `WIN_MAX_TOTAL_UNITS=100`（単勝合計）, `WIN_STAKE_UNIT=1000`,
`WIN_POOL_QUANTUM=1000`, `MIN_FIELD_TRIFECTA=8`, `MAX_FIELD=16`, `N_SIM=400000`,
`SCORING_PATCH_DATE/DEFAULT_TRAIN_FROM='2026/07/28'`, `VARIANCE_SHARE=0.8`, `STAT_RNG_WIDTH=0.03`,
`INTERACTION_SHRINK=0.5`, `SIGMA_SAFETY=1.0`, `MARKET_EDGE_RATIO=1.3`, `UNBET_ODDS=1.5`,
`DEFAULT_SETTINGS`（bankroll, kelly_fraction=0.25, edge_min=0.10, model_weight=0.7, min_prob=0.003 等）。

**関数（`oasis_core.py`）**
`parse_race_log`（logg解析）, `parse_unified`（ブックマークレット貼付の統合解析）,
`effective_stats`（パッシブ倍率畳み込み）, `build_features` / `feature_names`（ハイブリッド特徴量）,
`train_model`（学習＋σ校正＋校正チェック, bundleを返す）, `predict_base`,
`simulate_trifecta`（MC）, `analyze`（本体: 予測→単勝/3連単→EV→推奨, `res` を返す）,
`estimate_win_pool`（プール逆算＋1000スナップ）, `win_bet_picks_pool` / `win_bet_picks`（単勝配分）,
`diagnose_floor_odds`（オッズ1.5が「未投票」か「下限に張り付いた大本命」かを Σ(1/od) で判別）,
`_race_key` / `_base_key`（レース識別キー。同日同時刻の別レース衝突を防ぐ）,
`passive_effects`（パッシブの効き目表）, `passive_coef_table`（倍率表）, `model_formula`（距離別係数表）,
`internal_stat_weights`（内部式の実効重み）。

**bundle の主なキー**: `model, alpha, spec, feature_names, race_sigma, tri_sigma, sigma_mle,
race_spearman, top1_acc, n_races, calibration, calibration_tri, messages, warnings` など。

---

## 10. テスト

```bash
python3 selftest.py logg        # 解析→学習→予測→ベットログ→回帰テストの一気通貫
python3 e2e.py                  # analyze と AppTest 相当（作業dirにある場合）
node verify.js                  # 4種ブックマークレットの構文チェック（作業dirにある場合）
# Streamlit AppTest:
python3 -c "from streamlit.testing.v1 import AppTest; at=AppTest.from_file('oasis_app.py',default_timeout=200).run(); print(at.exception, at.error)"
```

機能追加時は必ず `CORE_VERSION` と `oasis_app.py` の `REQUIRED_CORE` を同時に上げること。

---

## 11. 未対応・注意点・今後の候補

- **乱数幅3%化の追従**: σは自動校正なので、変更後レースが貯まるほど正しくなる。過渡期は一時的に自信過剰に
  なりうる（安全弁で緩和）。`VARIANCE_SHARE=0.8` は「旧0.5・分散4倍」からの導出で、実際の荒れ具合で微調整可。
- **3連単専用σ**: 現在 8頭以上62レースで校正。データが増えたら再確認。σは膨らませない原則との区別に注意
  （tri_sigmaは"是正"であって"安全マージン"ではない）。
- **自動カバー買い**: 現状は「正直な確率＋買い方ガイド」で点数は人が選ぶ。「○%カバーまで自動購入」に
  寄せる余地あり（ユーザーに確認済みの選択肢）。
- **データ増強**: `harvest.js` で過去確定レースを採取し `logg/` に足すと精度が上がる（新式レースが鍵）。
- **到達性**: サーバは `api.oasis.red` に403。API取得は必ずブラウザ側で。WebFetch/curl等での代替取得は不可。

---

## 12. 付随物（ツールとは無関係）

- `oasis_kantan_score.html` / `oasis_kantan_score.pdf`（このリポジトリには含めていない、ユーザーPC側）:
  友達が電卓だけで概算できる「簡易スコア早見表」。内部式を丸めた重み（短距離 SP+PW×0.3+ST×0.2、
  マイル SP+(PW+ST)×0.5、中距離 SP+PW+ST、長距離 SP+PW+ST×1.5）＋主要パッシブ倍率。過去34/74レースで
  検証（1着的中71%）。**ツール本体には手を入れない**約束で作成した別物。

---

*このドキュメントは `repo/HANDOFF.md`。プロジェクトの状態が変わったら、変更履歴（§7）と定数/関数（§9）を更新すること。*
