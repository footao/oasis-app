# 厳格コードレビュー（2026/08/09）

対象: `oasis-app-v2_6`（CORE_VERSION 3.2.0）
レビュー範囲: `oasis_core.py` / `oasis_app.py` / `sheets_backend.py` / `drive_backend.py` / `selftest.py` / `make_formula_doc.py` / `bookmarklets/src/*.js` / `minify.py`

実施した機械検証:

- `py_compile` 全ファイル → **合格**
- `pyflakes` → 指摘1件のみ（`_order_loglik` の未使用変数 `n`、後述）
- `node --check` 4本のJS → **合格**
- 合成ログ30レースでの `parse → train → analyze` 一気通貫 → **成功**（学習7.1s / 解析0.1s、スピアマン0.867）
- 疑わしい箇所は小テストで実挙動を確認（後述の各項に明記）

総評: **全体の品質はかなり高い**。読み込み失敗時のログ保護、購入の二重送信ガード、口数上限の二重チェック、σ校正の設計判断の文書化など、過去レビューの学びがきちんとコードに反映されている。ただし今回、**実弾に直結しうる誤判定1件**と、**学習データを静かに汚染する2件**を新規に見つけた。以下、重大度順。

---

## 🔴 重大（お金・データの正しさに直結）

### R1. オッズ「1.50」の実投票馬を「未投票」と誤判定する

- 場所: `oasis_core.py` `analyze()` の unbet 判定（`abs(odds[i] - UNBET_ODDS) < 1e-9` 付近、L2262）、`market_win_prob()`（L1318: `odds > ODDS_FLOOR` で 1.5 を除外）、`probe.js` の `PH=1.5` 判定も同型。
- 問題: 1.5 は「未投票の初期値」であると同時に、**ゲームの最低オッズ（フロア）でもある**（コード内コメント自身が「2026/02/25: 最低オッズ 1.1→1.5」と書いている）。シェアが約 2/3 を超える大本命は、実投票があっても表示 1.50 になり得る。小さいプール（＝このツールが最も使われる場面）では十分起こる。
- 影響:
  1. `win_bet_picks_pool` がその馬を「投入額 0」として扱い、実効オッズを `(P+u)/u` と桁違いに過大評価 → **大本命に「超高配当」と誤認して買い推奨**を出しうる。
  2. `market_win_prob` がその馬を市場確率 0 として他馬に再正規化 → λ混合の p_bet も歪む。
- 修正案（検出可能です）: プール実測値があるとき `Σ(1/od)`（1.5表示馬を除く）を計算し、合計が 1 を大きく割る場合は「残りシェアが1.5表示馬に載っている＝実投票あり」と判定できる。少なくとも `win_pool` 実測時はこのクロスチェックを入れ、矛盾したら unbet 扱いを止めて警告を出すべき。probe.js 側でも同じ判定が可能（1.5の馬の `my_amount` 合計と他馬シェアの残差で分かる）。

### R2. harvest ログと Discord ログの併用で同一レースが二重カウントされる

- 場所: `parse_race_log()` の重複除去（L719: `subset=['race_key','name','owner','speed','stamina','power','rank']`）× `harvest.js` L60（owner を常に `@Unknown` で出力）。
- 問題: 重複除去キーに **owner** が入っているが、Discord 側は実オーナー名（または `unknown`）、harvest 側は `@Unknown`。さらに同名馬の表記も Discord 素名 vs harvest `名前#1` でずれる。つまり**期間が重なる harvest 出力を logg に足すと、重複除去は一切効かず同一レースが2重に入る**。
- 影響: race_key が同じため「1レース16行（8頭×2）」のような偽レースになり、`n_field` が倍増、レース内中心化・スピアマン・σ校正・学習重みがすべて歪む。HANDOFF は「harvest で logg を増やせ」と推奨しているので、**運用上ほぼ確実に踏む**。
- 修正案: 重複除去キーから `owner` を外す（name+stats+rank+race_key で実用上一意）。あわせて harvest 側の `名前#n` と Discord 素名の突合（`species_name` ベース）も検討。二重取り込み検知時は件数を warning に出す。

### R3. race_key 衝突で別レースが1レースに合成される

- 場所: `parse_entries`/`parse_results` の `r_key = f"{date} {r_time}"`（レース番号・guild を含まない）、`harvest.js` L35 の `race_time || '0:00'` フォールバック。
- 問題: 同日同時刻の別レース（別ギルド・レース番号違い・harvest で time が取れず `0:00` になった複数レース）が**同一 race_key に合流**する。合流すると R2 と同様に中心化と n_field が壊れる。正規表現は `第(\d+)レース` を取っているのに race_key に使っていない。
- 修正案: race_key に race_no（あれば）を含める。harvest 側は `0:00` フォールバックをやめ、time が無いレースは schedule_id をキーに含める。
- 関連: `_date_before` は直前 2500 文字しか遡らないため、日付が拾えないと `'????'` → `_d=NaT` → 学習フィルタで**無言で捨てられる**（`_recent_races` のソートも NaT で順序が不定になる）。捨てた件数を warning に出すべき。

---

## 🟠 中（潜在バグ・信頼性）

### M1. `analyze()` の設定マージが None で既定値を潰す

- 場所: L2046 `s.update({k: v for k, v in settings.items() if v is not None or k in DEFAULT_SETTINGS})`
- 検証済み: `settings={'model_weight': None}` を渡すと `s['model_weight']` が None になり、後段の `float(...)` で TypeError になることを実行確認した。条件は「既知キーなら None でも上書き」になっており、意図（「None は無視」）と逆。現行の呼び出し元は踏んでいないが、時限爆弾。`if v is not None` だけで良い。

### M2. 同一シードで同一シミュレーションを2回実行している

- 場所: `analyze()` L2162-2168。`win_p` 用と `combo_prob` 用で `simulate_trifecta` を2回呼ぶが、`tri_sigma == sigma` のとき2回目は**シードも入力も同じ＝結果が完全に一致**（実行して同一性を確認済み）。1回目の戻り値の combo を捨てているだけ。
- 影響: 正しさには無害だが、既定 40万試行の MC を**常に2倍**回している。`tri_sigma == sigma` のときは1回の呼び出しで `win_p, combo_prob` を両方受ければ半減する。σが異なる場合も、win用の combo 計算（`counts` 集計）は捨てる前提なら `n>=3` の集計をスキップできる。

### M3. `_fetch_pool_api` はサーバから到達不可能なのに毎回5秒ブロックする

- 場所: L1613。HANDOFF 自身が「このサーバは api.oasis.red に 403」と明記している。貼り付けに `pool=` が無く guild/schedule_id があると、**解析のたびに必ず5秒待って必ず失敗**する。到達不可が既知なら既定で呼ばない（またはフラグ化・timeout短縮）べき。

### M4. ベットログの精算が「購入時点のオッズ」で払い戻しを計算する

- 場所: `BetLog.settle()` L2702。パリミュチュエルなので最終オッズは締切時に変わるが、`payout = 記録時 odds × stake` で確定させている。単勝は記録値が推定の `eff_od`。つまり**実績レポートの ROI・損益は系統的にずれる**。モデル校正（予測 vs 実測的中率）には影響しないが、「市場に勝てているか」の検証には使えない数字になっている。最低限、レポートに「払戻は購入時オッズ換算の概算」と明記するか、精算時に実払戻額を入力できる欄を足すべき。

### M5. `st.stop()` が画面全体を止める

- 場所: `oasis_app.py` L844（単勝記録の重複エラー時）。`st.stop()` はそのラン全体を止めるので、**エラー表示以降のログタブUI（精算・レポート・一覧）が丸ごと消える**。3連単側（L829）は else で正しく処理しており、非対称。else に揃えるだけで直る。

### M6. Community Cloud では `passive_spec.json` への学習保存が消える

- 場所: `merge_passive_spec` → `_spec_path()`（アプリと同じ場所）。Streamlit Community Cloud のFSは再起動で消えるため、「📘 取り込みました。再学習すると反映されます」と案内した内容が**リブート後に消える**。seed が35種全部を持っている現状では実害は小さいが、新スキル追加時に「取り込んだはずが消える」混乱の元。Sheets/Drive がある構成なのでそちらに退避するのが筋。

---

## 🟡 軽微

1. **`bare()` が `#1`（スペース無し）を剥がさない**（L1343、`DUP_MARK=' #'` 前提）。ブックマークレットの同名馬表記は `名前#1` なので、素名フォールバック層はこの形式に対して実質機能していない。現状は完全一致で救われているが、`_SPECIES_RE`（両対応）と挙動が食い違うのは罠。
2. **`_order_loglik` の未使用変数 `n`**（L860、pyflakes 指摘）。
3. **`_train_cached` に docstring が2つ**（`oasis_app.py` L169-170）。2つ目は無意味な文字列式。1つ目の内容（「_texts はキャッシュキーに含まれない」）は `content_key` 追加で解決済みの古い注意書きに見えるので整理を。
4. **プール実測エキスパンダ内の死にコード**（`oasis_app.py` L545-552）: `order` 辞書は未使用、`if i < len(disp2): pass` は無意味。この周辺は「ha の並び＝analyze の並び」という暗黙依存で `my_units` を組んでおり、コメントで保証根拠を書いておくべき。
5. **`undo_last` は最後の race_id の全行（精算済み含む）を消す**。「直近レースを取消」の表記どおりではあるが、精算済みまで消えることは UI で一言警告した方が安全。
6. **minify.py の行末コメント除去はヒューリスティック**（クォート偶数判定＋末尾8文字の `://` チェック）。正規表現リテラルや `" // "` を含む文字列で誤切断し得る。verify.js が最後の砦なので、ビルド後の verify を必須手順として README に固定を。
7. **`harvest.js` のコメント「連続で欠損が続いたら打ち切り」は未実装**。常に N レース叩く。
8. **`probe.js` の `ownNow`**: オッズが動かず再取得に失敗した場合 `pets1` が古いままで、`|| (own+spent)` は reduce が 0 のときしか効かない。own>0 かつ再取得失敗だと spent 分が欠ける。
9. **`mc_note` が単勝σしか表示しない**（L2329）。3連単は tri_sigma で回しているので、表示も2本立てに。
10. **logg の Discord エクスポートには実ユーザー名が含まれる**。GitHub リポジトリが public なら個人ハンドルの公開になる。private 確認を。

---

## ✅ 良かった点（維持すべき設計）

- `BetLog.load(strict=True)` → 書き込み前の必須読込で「読めないのに空と誤認して全消し」を構造的に防いでいる。Sheets 側の「update→末尾clear」の順序も同思想で一貫。
- buy.js の `executed` ガード・上限の事前ブロック・通信エラー時の「送信済みか不明」表示は、実弾ツールとして正しい防御。
- σを膨らませない原則、λ混合、min_prob の三点セットと、その**理由がコード内コメントに残っている**こと。tri_sigma の分離根拠（8頭以上・順番的中で校正）も明快。
- app/core の版チェック（`REQUIRED_CORE` + `_NEEDED` の属性検査）は、配布運用の事故を実際に潰せる作り。
- 同名馬の確率コピー防止（`_bare_unique` 判定）、プール実測の od² 加重・1000rrc スナップの理屈も筋が良い。

---

## 推奨する着手順

| 順 | 項目 | 理由 |
|---|---|---|
| 1 | R2/R3（データ層の二重カウント・キー衝突） | harvest 運用を始めた瞬間から学習が静かに汚染される |
| 2 | R1（1.5 フロア誤判定） | 実弾の買い推奨が桁違いに誤る可能性。プール実測時のクロスチェックで検出可能 |
| 3 | M1（None マージ） | 1行修正。時限爆弾の除去 |
| 4 | M4（精算オッズ） | 「市場に勝てているか」の検証はこのログが頼りのため |
| 5 | M2/M3 | 体感速度（MC半減・5秒ブロック除去） |

テスト面の提案: `selftest.py` は正常系の一気通貫のみで、**お金を計算する関数（`allocate_units_stable` / `optimal_units_ev` / `win_bet_picks_pool` / `estimate_win_pool`）の境界ケースが無検証**。R1〜R3 の再発防止も含め、(a) 1.5フロア馬入りの単勝配分、(b) harvest+Discord 重複ログの取り込み、(c) None入り settings、の3ケースを selftest に追加することを勧める。
