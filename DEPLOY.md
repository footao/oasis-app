# Streamlit Community Cloud へのデプロイ

学習ログは**リポジトリから読むのが既定**です。書き込みが必要なベットログだけ、
Streamlit Cloud がファイルを保存できない（再起動で消える）ため Google スプレッドシート
に置きます。

| データ | 置き場所 | 更新のしかた |
|---|---|---|
| 学習用レースログ（Discordエクスポート） | **リポジトリの `logg/`**（既定） | ファイルを差し替えて push |
| ベットログ（賭け・結果） | **Google スプレッドシート** | アプリから自動で書き込み |
| パッシブの数値（35種） | リポジトリの `passive_spec.json` | 通常は不要（新スキル追加時のみ） |

ベットログだけは Cloud のファイルシステムが消えるためスプレッドシートに置きます。
学習ログは読むだけなので、リポジトリに置いておけば十分です。

アプリのサイドバー「ログの取得元」で3つから選べます。既定は **📁 リポジトリ内**です。

| 取得元 | 使いどころ |
|---|---|
| 📁 リポジトリ内（既定） | 普段はこれ。`logg/` の .txt をまとめて読みます |
| ⬆ アップロード | push せずにその場で新しいログを試したいとき |
| ☁ Google ドライブ | push せずにログを差し替えたいとき（下記の設定が必要） |

---

## 1. ログをリポジトリに置く（既定の運用）

`logg/` フォルダに Discordエクスポートの `.txt` を入れて push するだけです。
フォルダ内の .txt はすべてまとめて読み込まれます。

ログを更新するときは、`logg/` のファイルを新しいエクスポートに差し替えて push し、
アプリで **「📚 モデル学習 / 再学習」** を押してください。

> 古いエクスポートを残したまま新しいものを足しても大丈夫です。
> 期間が重なる分は自動で重複除去されます（学習メッセージに「重複◯行を除外」と出ます）。

---

## 2.（任意）Google ドライブから読む

1. ドライブに `oasis-logs` などのフォルダを作り、Discordエクスポートの `.txt` を入れます。
   複数ファイルを入れてOK（全部まとめて学習します）。
2. そのフォルダを右クリック → 共有 → **サービスアカウントのメールアドレス**
   （`xxx@your-project.iam.gserviceaccount.com`）を**閲覧者**で追加。
3. フォルダを開いたときのURLから ID を控えます。

   ```
   https://drive.google.com/drive/folders/1AbCdEfGhIj...   ← この末尾がフォルダID
   ```

   URL をそのまま secrets に貼っても動きます（IDを自動で抜き出します）。

> Google Cloud 側で **Drive API** が有効になっている必要があります。
> スプレッドシート連携の時点で有効化済みのはずですが、
> エラーが出たら「APIとサービス」で `Google Drive API` を確認してください。

push せずにログを差し替えたい場合だけ設定してください。不要ならこの節は飛ばせます。

### secrets の設定

`.streamlit/secrets.toml.example` をひな形にしてください。
既存の設定に `[gdrive]` セクションを足すだけです。

```toml
[gcp_service_account]
...（既存のまま）...

[gsheets]
spreadsheet_id = "..."
worksheet = "bet_log"

[gdrive]
folder_id = "https://drive.google.com/drive/folders/1AbCdEfGhIj..."
pattern = ".txt"
```

- **ローカルで試す場合**: `.streamlit/secrets.toml` として保存（`.gitignore` 済み）。
- **Cloud の場合**: アプリの ⚙ Settings → **Secrets** に同じ内容を貼り付け → Save。
  保存すると自動で再起動します。

設定すると、サイドバーの「ログの取得元」に **☁ Google ドライブ** が選べるようになります
（既定はリポジトリのままなので、明示的に選んだときだけ使われます）。

---

## 3. デプロイ

```bash
git add -A
git commit -m "v2: パッシブ2枠・新スコア式・実数値スペック対応"
git push origin main
```

Community Cloud は push を検知して自動で再デプロイします。
初回だけ `requirements.txt` の変更（`gspread` / `google-auth` 追加）で
少し時間がかかります。

---

## 動作の確認ポイント

アプリを開いたら、サイドバーの上に取得元が出ます。

- `📁 /mount/src/oasis-app/logg` → リポジトリから読めています（正常）
- `❌ 見つかりません:` → `logg/` が push されていないか、パス欄の値が違います
- `☁ ファイル名...` → ドライブから読めています

「📒 実績ログ」タブの先頭に保存先が出ます。

- `🗂 保存先: Google スプレッドシート` → OK
- `🗂 保存先: ローカルCSV` の警告 → secrets が読めていません

## ログを差し替えたいとき

**既定（リポジトリ）の場合**

1. `logg/` のファイルを新しいエクスポートに差し替えて push。
2. 再デプロイ後、アプリの **「📚 モデル学習 / 再学習」** を押す。

**push したくないとき**

- サイドバーの取得元を **⬆ アップロード** にして `.txt` をドロップ。その場で学習します。
- ドライブを設定している場合は **☁ Google ドライブ** を選べば、
  ドライブ上のファイルを差し替えるだけで反映されます（更新時刻を見ています）。

## キャッシュについて

学習結果は `st.cache_resource` でキャッシュされます。
初回は数十秒かかりますが、2回目以降やスライダー操作では再学習しません。
キャッシュの鍵はログの内容（ドライブなら更新時刻）と学習パラメータなので、
**ログを差し替えるか設定を変えると自動的に学習し直します**。

## 注意点

- **パッシブの数値**（`passive_spec.json`）はリポジトリのファイルを読みます。
  Cloud 上でアプリが新しいスキルの説明文を覚えても、それは**そのセッション限り**です。
  恒久的に残したい場合は、ローカルで貼り付けて更新された `passive_spec.json` を
  コミットしてください（現状35種すべて入っているので、当面は不要です）。
- スプレッドシートは**全体を読んで全体を書き戻す**方式です。
  複数人が同時に精算すると後勝ちで上書きされます。個人利用なら問題ありません。
- ベットログの列に `bet_type`（3連単 / 単勝）が増えました。
  旧シートをそのまま使っても、読み込み時に `3連単` を補うので移行作業は不要です。
- サービスアカウントのメールに共有していないファイルは読めません。
  「見つかりません」系のエラーが出たら、まず共有設定を確認してください。
