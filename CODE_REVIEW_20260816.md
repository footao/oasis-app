# コードレビュー 2026/08/16

3方向（お金の経路 / 新規スクリプト群 / UIとJS）を並列で検証。**主張はすべてコードを読み、
実データで再現してから採用**しています。再現できなかったものは末尾に「誤検出」として残しました。

---

## A. すでに直したもの（検証済み・repo に書き込み済み）

### A-1. 市場確率 q が一律 1/補正倍率 に縮んでいた 〔重〕〔今日私が入れた〕

`oasis_core.py:2747` の `inv_sum` を**補正後**のオッズで計算していました。補正で全オッズが
`_f = P/(P−20万)` 倍になるので `Σ(1/od)` が `1/_f` まで落ち、`max(inv_sum, 1.0)` のクランプに
当たって、**すべての組の市場確率が一律 1/_f 倍**に縮んでいました。

```
プール40万・完全な市場（賭け金合計20万）
  補正前 Σ(1/od) = 1.000     ← 本来こうなる
  補正後 Σ(1/od) = 0.500     ← クランプに当たる
  結果: 本来 q=0.40 の組が q=0.20 として扱われる（全組ちょうど半分）
```

λ混合 `p_bet = 0.7·model + 0.3·q` の**市場側が実質無効化**されます。方向は保守側
（エッジ過小 → 買い控え）ですが、**プールが薄いレースほど強く効く**ため、
初期プール金の補正で拾おうとした場面を自分で潰していました。

補正前オッズで正規化し、分子だけ `_f` を掛けて戻す形に修正。回帰テスト **P2（4件）** で固定。

### A-2. `settle_bets.py` が下限オッズ 1.0 を最終オッズとして採用 〔重〕〔今日私が入れた〕

`Σ(1/od)` のゲートだけでは足りず、実データの 2レースが通過して **1.0倍** を返していました。

```
sid 1981  odds [1.0, 49.5]              Σ=1.020 → 1.0 を返す
sid 1897  odds [1.0, 104.4×3]           Σ=1.029 → 1.0 を返す
```

`BetLog.settle` はこれを受け入れて**購入時オッズを 1.0 で上書きし** `payout_kind='実績'` と記録。
的中したのに払戻＝賭け金（利益ゼロ）になり、**元のオッズも消えます**。
`1.0` も `1.5` も価格ではなく下限表示値です。

`min(vals) <= 1.0` と「全馬同値」の2条件を追加。`--selfcheck` に実データの形を4件追加。

### A-3. `harvest_results.py` が失敗しても終了コード 0 〔中〕〔今日私が入れた〕

`main()` を裸で呼んでいて戻り値を捨てていました。`harvest_daily.bat` は
`exit /b %ERRORLEVEL%` を返すので、**タスクスケジューラが「成功」と記録**します。

`--forward` で races.jsonl が無い/空のとき（初回、ファイル移動後）に `[NG]` を出して
何も採らずに成功扱い、が永久に続きます。`sys.exit(main() or 0)` に修正。

### A-4. スタミナ定数の突き合わせキーが衝突していた 〔中〕

`stamina_report.py` の `(日付, 馬名)` キーは1日6レースあるため後のレースが前を上書き。
**3,389頭中 1,025件が上書き**され、うち距離まで一致して素通りするものが 410件。

キーに時刻を足して衝突ゼロに。突き合わせ **999 → 1,225頭**、残スタミナの検算
**79.9% → 86.7%**。定数を測り直しましたが**変化はごく小さく**（c は1〜2%、境界は短距離のみ
1.955→2.125 / 2.967→2.879）、予測への影響はほぼありません。クランプ境界は多数の馬が
張り付いて決まるので、上書きに対して頑健だったためです。

### A-5. 「表示od」列が補正後の値を指していた 〔中・UX〕

3.7.0 で中身を補正後に変えたのに列名が「**表示**od」のまま。ゲーム画面が2.0のときツールが
22.0と出るので、壊れていると誤解するか、逆に画面が22倍だと誤解します。
「実od(補正後)」に改名し、補正倍率が1.001を超えるときだけ差の説明を出すようにしました。

### A-6. ドキュメントの矛盾 2件

- `HANDOFF.md` が「`harvest.js` で過去レースを採って logg に足すと精度が上がる」と
  推奨したまま。§3-b の「by-id のステータスは腐る」と**正面から矛盾**します。
  しかも logg 書式には `harvested_at` が無いので `load_races(need_stats=True)` でも弾けません。
- `stamina_budget()` の docstring が「余りは完全に無駄」＝実測と逆。

---

## B. 未着手（重い順）

> **進捗**: **B-1〜B-8 すべて修正済み**（2026/08/16）。以下は記録として残す。

### ~~B-1. 実効オッズが他の組に置いた口数を無視~~ ✅ 2026/08/16 修正済み

`oasis_core.py:2014 ev_c` / `1991 optimal_units_ev`:
`eff = (P + k·u)/(Pc + k·u)` の分子が**その組の口数しか足していない**。実際はプールは
全組の合計ぶん増えます。**単勝側は正しく実装されています**（`win_bet_picks_pool` 2261行:
`P_new = pool + kv.sum()*stake_unit`）。つまり流儀の違いではなく片手落ちです。

実測: P=40万・3組・計7口 → 本命の eff 6.14（コード） vs 6.71（正）、合計EV −23%。
**払戻を過小評価するので張り不足**になり、口数配分も歪みます。

### ~~B-2. `autopilot.js` の市場確率が取得できた40組だけで正規化~~ ✅ 2026/08/16 修正済み

`autopilot.js:245`。Python は全組で合計して `max(inv_sum, 1.0)` でクランプしますが、
JS は取得した40組の確率を**強制的に合計1にします**。12頭立て（1320通り）で40組が
市場の55%しか持たない場合、q が約1.8倍に膨らみ、エッジが +0.06 過大（`EDGE_MIN` は 0.10）。
**オッズ取得がレート制限で落ちるほど悪化**します。

### ~~B-3. `autopilot.js` の `PENDING` が期限切れしない~~ ✅ 2026/08/16 修正済み

`autopilot.js:331,342`。🛒 を押さないと `PENDING` が残り続け、**以降レースを一切解析しません**
（画面はカウントダウンし続けるので気づけない）。また `今すぐ解析` は `force` を通すので、
購入済みレースをもう一度組める＝1レース3口の上限を回避できます。

### ~~B-4. `BetLog._save` が非アトミック~~ ✅ 2026/08/16 修正済み

`to_csv` で上書き。書き込み途中で落ちると**途中までのCSVが正常に読める**ので、
`strict=True` のガード（例外しか捕まえない）を素通りして、次の書き込みで欠損が確定します。
実測: 5行 → 途中切断 → 2行として読めて → 3行で保存、3レース消失。
`to_csv(tmp)` → `os.replace` で直ります。

### ~~B-5. 学習キャッシュが `passive_spec.json` を見ていない~~ ✅ 2026/08/16 修正済み

`oasis_app.py:167`。`_train_cached` のキーはログ本文だけですが、`train_model` は
passive_spec を**ファイルから読み**、`analyze` はパッシブ説明文を貼るたびに**そのファイルを書き換えます**。
ゲーム側がパッシブの数値を変更 → 貼り付けで取り込み → 再読み込みしても、
**古い倍率で学習したモデルが黙って返ります**。📚ボタンを押したときだけ反映。

### ~~B-6. 貼り付けたレースを変えても「古い結果です」が出ない~~ ✅ 2026/08/16 修正済み

`oasis_app.py:494`。`ss.result_settings` はサイドバー設定しか見ていません。
📋 貼り付けボタンはテキストを差し替えますが `ss.result` を消しません。
**レースBのデータを貼った画面に、レースAの推奨と schedule_id が無表示で残ります**。
そのまま記録するとレースAの買い目がログに入ります。`hash(raw_text)` を混ぜれば1行で直ります。

### ~~B-7. 非有限オッズで `analyze()` が落ちる~~ ✅ 2026/08/16 修正済み

`oasis_core.py:2020`。`inf` のオッズがガードを通過して `int(nan // stake)` で ValueError。
貼り付けCSVに `inf` があると画面が落ちます。`math.isfinite(od)` の追加で解決。

### ~~B-8. 分析スクリプト群の細かい不整合~~ ✅ 2026/08/16 修正済み

- `fit_stamina.py` / `evaluate.py` が必要スタミナを `cs[0]×区間数` で計算（`stamina_report.py`
  はコメントで「これは誤り」と書いたうえで実測を合計）。検算の一致率が 65.8% と出ますが、
  正しく合計すれば **100%** です。「式が半分しか合っていない」と読める嘘の数字。
- `fit_ridge_stamina.py` が `costs[0]`、`stamina_report.py` が `need/n_seg` で **同じ定数を
  8〜13%違う値**で出す。今の `oasis_core` は後者と一致。前者を貼ると必要STが1割ずれます。
- `make_stamina_table.py:42` が `/tmp/style.css` をハードコード（Windowsでは即クラッシュ）。
- `evaluate.py` の列Cが、罰則が決まらない距離では**Bと同一なのに「B＋罰則」と表示**。
- `anonymize_logg.py` は2回目の実行で匿名化済みテキストを `.bak` に上書き（原本消失）。
- 呼び出しゼロの関数3つ: `normalize_passive` / `simulate_rankings` / `BetLog.race_exists`。

---

## C. 誤検出だったもの（記録として）

前回のレビューで上位2件が誤検出でした。**私の作業コンテナだけに残っていた古い生成物**
（`deploy/`、古い `autopilot.bundle.js`）を見て「3.7.0がバンドルに入っていない」と判定したものです。
実際の GitHub 上のバンドルには `trifecta_pool_seed:200000` / `MIN_POOL:300000` /
`core_version 3.7.0` が入っていることを確認済み。**リポジトリ側は正常でした。**

同様に「`src/` の重複はもう無い」も誤りで、**実リポジトリには今も存在します**
（HANDOFF §0 の警告は正しい）。

---

## D. 検証して問題なしだったところ

- **初期プール金の補正は一度だけ適用**。`csv_odds_raw` の退避と `resolve_payout_pool` への
  受け渡しは正しく、キャリーオーバーの二重計上は起きない。
- **口数上限はすべて守られている**。3連単20口・1組20口・単勝100口（購入済み分を含めても）。
- **Python↔JS のスタミナ収支は完全一致**（360頭×4距離×2馬場、最大誤差 3.5e-10）。
  定数はすべて `model.json` 経由で、JS側に数値の二重定義なし。
- **モンテカルロも一致**（組確率の最大差 0.0027 ＝ 乱数ノイズの範囲）。
- **`autopilot.js` は自動実行しない**。`doBuy` はボタンからしか到達せず、二重送信ガードも有効。
- `optimal_units_ev` の閉形式を再導出して正しいことを確認。
- `estimate_win_pool` の誤差伝播（デルタ法）と1,000rrc格子スナップは妥当。
- `diagnose_floor_odds` は空・全NaN・全下限のいずれでも安全側（NaN）に倒れる。
- `analyze()` を10種の壊れた入力でファズしたが例外は出ず、すべて `{'ok': ...}` を返す。
- 単勝側に初期プール金は一切混入していない。

---

## E. 次にやるなら

A・B とも全件対応済み。**次に手を入れるならここ**（レビューで挙げなかったが積み残し）:

1. **`src/` とリポジトリ直下の重複ファイル削除** — `model.js` が3箇所にあり、
   `build_autopilot.py` が読むのは `bookmarklets/src/` だけ。間違った方を直して
   「反映されない」と悩む事故が待っている。`git rm` するだけ。
2. **λ（model_weight）の実測** — ベットログが20件貯まったら `fit_model_weight.py`。
   いまは市場オッズが下限1.5だらけで測れない。
3. **3連単の校正** — 予測45% vs 実測46% まで来た。レースが増えたら再確認。
4. **`activated_passives` の活用** — timeline に1,493件の発動記録がある。
   セカンドウインド(6件)・ペース配分(5件)のような推定の粗いパッシブを、
   着順からの逆算ではなく発動記録そのものから測れる。
