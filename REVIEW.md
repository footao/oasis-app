# 全体レビュー（2026/08/03）

コード・モデル・運用方針を根本から見直しました。**確定バグ23件を修正**し、
モデルの妥当性については **「内部精度は本物だが、市場に勝てるかは未検証」** という
結論に達しています。

---

## 1. 見つかって直したもの（重大な順）

### 🔴 お金が意図せず出ていくもの

| # | 症状 | 何が起きるか | 状態 |
|---|---|---|---|
| 1 | **一括購入の二重発注** | 購入完了後にチェックを外して入れ直すと、同じ内容がもう一度全額送信される。警告も出ない | 修正・検証済 |
| 2 | **口数上限が警告だけで止まらない** | 3連単40口（40万rrc）を貼れば警告は出るが**そのまま送信できた**。既存購入分（`my_amount`）も見ていなかった | 修正・検証済 |
| 3 | **締切済みでも送信していた** | 警告のみでPOSTを投げていた | 修正・検証済 |
| 4 | **同名馬を素名で指定すると別個体に賭ける** | `mio` が2頭いる時、`mio x5` が1頭目に確定送信されていた（APIが `display_name` を返す場合） | 修正・検証済（曖昧なら拒否） |
| 5 | **通信エラー時に「使っていない」と表示** | 送信済みかもしれないのに「使用 25,000」と出て、買い直すと二重払いになりうる | 修正済（不明額を別表示＋購入済み合計を再取得して照合） |

### 🔴 データが消えるもの

| # | 症状 | 状態 |
|---|---|---|
| 6 | **ベットログの読み込み失敗を握りつぶし、次の保存で全消去** | `load()` が例外を握って空DataFrameを返し、`record`/`settle` がそれで全体を上書きしていた。スプレッドシートが一時的に読めないだけで**全履歴が消える** | 修正・検証済（読めなければ `BetLogReadError` を投げて書き込みを中止） |

### 🔴 予測・金額が誤るもの

| # | 症状 | 状態 |
|---|---|---|
| 7 | **10着以降の着順が壊れていた** | 正規表現の先読みが「10着」の `1` と `0` の両方で分割し、10着→rank 0、11着→rank 1 … になっていた。実ログで rank 0 が433件、rank 1 が1,273件（レース数900に対し373件過剰）。学習データが直接汚染されていた | 修正・検証済（rank 0 が0件、最大rank 16） |
| 8 | **同名馬の確率を別個体にコピー** | 素名フォールバックで、勝率0.0000%の `mio#2` の買い目に `mio#1` の確率が入り、**同額が配分されていた**（検証例で12口中6口＝6万rrcが無価値な組へ） | 修正・検証済 |
| 9 | **単勝の記録金額が10倍** | 3連単の単価（10,000）で保存され、ROI・損益・校正がすべて10倍ずれる | 修正・検証済（1,000/口） |
| 10 | **単勝プールの二重計上** | 購入済み口数をプールに足していたため希薄化を過小評価。検証例で実効オッズを **4.5倍** 過大評価＝過剰投資 | 修正・検証済 |
| 11 | 未成立スリーブが資金リスク上限を無視 | 資金15万・上限1口の設定でも計5口（資金の33%）を推奨していた | 修正済 |
| 12 | 未知の距離で全馬同じ確率 | `1600m` のような表記だとステータスが一切効かず、無警告で無意味な推奨を出していた | 修正済（エラーで中止） |
| 13 | 購入画面の先頭ブロックで馬名を取り違え | ページのヘッダを馬名として拾っていた | 修正・検証済 |
| 14 | `optimal_units_ev` の切り捨て | 最適口数を1つ外し、EVを最大10%取りこぼしていた | 修正・検証済 |
| 15 | 同名馬でプール推定が失敗 | 名前キーの辞書で1頭消え「試し買いが見つかりません」になっていた | 修正・検証済 |
| 16 | ステータス欠損行を無警告で除外 | 頭数が変われば全確率がずれるのに黙って落としていた | 修正済（警告） |

### 🟠 表示・運用が誤解を生むもの

| # | 症状 | 状態 |
|---|---|---|
| 17 | **キャッシュキーにログ本文が入っていない** | `logg/` を差し替えても「再学習」を押すまで**永久に古いモデル**が返り続けていた | 修正済（内容ハッシュを鍵に追加） |
| 18 | 設定変更後も古い推奨が残る | 資金を変えても前の設定の購入リストが表示され、そのまま買えてしまう | 修正・検証済（警告） |
| 19 | 単勝の推奨が2箇所に出て合計が上限超過 | 簡易版（100口）と実測版（95口）が同時表示 | 修正済（見出しで区別＋未測定時は警告） |
| 20 | 単勝のみのレースが永久に精算不能 | 着順候補が1頭しか出ず、pendingのまま残りID再利用も不可に | 修正済 |
| 21 | 記録ボタンの重複ガードが非対称 | 単勝は連打で重複、逆に単勝を先に記録すると3連単が弾かれる | 修正済 |
| 22 | σ校正が最大591MBを確保 | Streamlit Cloud（1GB）でOOMの恐れ | 修正済（レースごとに乱数を使い捨て） |
| 23 | Driveのページング欠落 | 200ファイル超で無言の切り捨て | 修正済 |

### 残っている既知の弱点（実害が小さいので未対応）

- 単勝オッズ `1.5` を「未投票」と判定するヒューリスティックは、本当に1.50になった馬を誤分類しうる
- 同着があるレースで「真の着順」を `argsort`（非安定ソート）で作っている。900レース中56レースに同着があり、3レースで並びが実際と食い違う。σ校正がわずかに歪む
- スプレッドシート書き込みが `clear()` → `update()` の非アトミック。clear成功後にupdate失敗すると空になる
- `@st.cache_resource` 内でエラーを `session_state` に書いているため、2人目以降のセッションにはエラーが出ない

---

## 2. モデルは信用できるか

### 内部精度は本物です

「1着的中94%」が過学習でないかを、**時系列で**検証しました（過去のレースだけで学習して未来を予測）。

| 検証方法 | レース内順位相関 |
|---|---|
| leave-one-race-out | 0.916 |
| **時系列（過去のみで学習）** | **0.914** |

ほぼ同じです。未来予測でも落ちないので、**内部精度は本物**と判断できます。
パッシブの公式数値を組み込んだ効果も本物です。

### しかし「市場に勝てるか」は別問題で、未検証です

これが最重要の結論です。最終単勝オッズが残っている**旧スコア式204レース**で比較しました。

| | レース内順位相関 | 1着的中 |
|---|---|---|
| モデル | 0.516 | 34.8% |
| **市場（人気順）** | **0.575** | **52.0%** |

旧式では**市場のほうが強い**。モデルの本命に毎レース1口ずつ賭けると **ROI −18.2%**、
さらに「エッジが大きい買い目だけ」に絞ると **−24%〜−38%** と悪化します。
これは「モデルが見つけた割安」が実際には**モデル自身の誤差**だった、という典型的な兆候です。

**新スコア式でこの比較ができるレースは2件しかありません。** 新式のモデルは内部精度が
0.74→0.92 と大きく改善しているので旧式の結果がそのまま当てはまるとは限りませんが、
**「勝てる」という証拠も現時点ではゼロ**です。

### 構造的な上限

単勝は控除0%のパリミュチュエル（実ログ379レースで `Σ(1/最終オッズ)=1.000`）＝
**他人の負け金を取りに行くゼロサム**です。的中時の利益は `x(B−B_w)/(B_w+x)` で、
どれだけ賭けても **他人が負け馬に入れた金額を超えられません**。

実際に観測したプールは race=1845 で **4,000 rrc、しかも全額が自分の掛け金**でした。
この状態では数学的にどう買っても負けます。

---

## 3. 推奨する使い方

### いますぐ実弾を入れるのは勧めません

代わりに**ペーパートレード**をしてください。ツールにその機能があります。

1. 推奨が出たら「📒 実績ログ」に**記録だけして買わない**
2. レース後に実着順を入れて精算
3. **20〜30レース**貯めて「📊 成績レポート」を見る

見るべきは2点です。

- **校正**: 「予測」と「実測」が近いか。乖離していればσを調整
- **ROI**: プラスか。ここがプラスにならない限り、実弾を入れる根拠がありません

### 同時に確認してほしいこと

**他人のプールが実際にいくらあるか**。プール実測ブックマークレットで
`win_own`（自分の分）と推定プールを比べてください。他人の金が数万rrc規模に育つレースが
常時あるのか、それとも4,000rrcのようなスカスカが普通なのか。後者なら、
そもそも稼げる土俵がありません。

### 3連単のほうが見込みがあります

- プールがAPIで直接取れる（推定不要＝誤差なし）
- キャリーオーバーがあり実効オッズが跳ねる
- 当たりにくいぶん他人の負け金が溜まる

ただし**3連単は過去オッズがログに残らないため、バックテストが一切できません**。
これも実測で確かめるしかありません。

### σ安全係数は下げないでください

現在1.25（弱気側）です。校正チェックは「予測63% vs 実測94%」と安全側に出ていますが、
これは**学習データ内**の話です。実績ログでROIがプラスだと確認できるまでは
1.25以上のままにしてください。

---

## 4. 更新方法

`oasis-app-v2.zip` の中身を**まとめて**アップロードしてください。
`oasis_app.py` と `oasis_core.py` は必ずセットです（版チェックが入っています）。

ブックマークレットは**3つとも差し替えが必要**です（購入・プール実測に重大な修正が入っています）。
