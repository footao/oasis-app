# レビュー引き継ぎ資料（2026/09/02）

**このファイルの目的**: 別のエージェント／開発者に「おあしすっち予想ツール」の**全体レビュー**を
依頼するために、前提・経緯・確定済みの判断・未解決点を1枚にまとめたもの。
まずこれを読み、次に `HANDOFF.md`（総合仕様）→ `REVIEW.md`（設計判断の履歴）の順に読むこと。

---

## 0. レビューしてほしいこと

金が動くツールなので、**壊れていることに気づかないまま賭け続ける**のが最悪のシナリオ。
優先順位はこの順:

1. **正しさ**: 期待値・確率・配分の計算に誤りがないか。特に Python と JS の**二重実装のズレ**。
2. **資金の安全**: 上限・下限・フィルタが本当に効いているか。境界値（口数上限、予算切れ、頭数不足）。
3. **統計の作法**: σの推定、OOF評価、学習データの選び方にリークや自己満足がないか。
4. **運用の壊れやすさ**: 自動購入（ブックマークレット）がサイト側の変更・通信失敗でどう転ぶか。
5. その後で可読性・重複・死んだコード。

**蒸し返さなくてよい判断は §5 にまとめてある**（過去に実測して決着済み）。
反論するなら「数字で」お願いしたい。

---

## 1. 30秒サマリ

- Discord上の育成レースゲーム「おあしすっち」で、出走馬のステータス・パッシブ・装備から
  着順を予測し、**3連単**と**単勝**の期待値ベースの買い目を出す。実際に賭けている。
- ロジック核 `oasis_core.py`（UI非依存）＋ Streamlit UI `oasis_app.py`。
  同じ計算の **JS移植**が `bookmarklets/src/*.js`（ブラウザ上で動く自動購入＝オートパイロット）。
- 現在の版: **CORE_VERSION 3.20.0** / **AP_VER 1.14.0** / `model.json` は
  **194レース学習・2026-09-02 ビルド**。
- 作業フォルダ `C:\oasissfable\repo` = GitHub `footao/oasis-app`（public, main）のクローン。
  `HEAD = 1d419bf` にレビュー対象がすべて入っている（育成プランナー3ファイルを除く。§2）。

```
Discordログ / result API
        │  train_model（Ridge, レース内で中心化した log(score)）
        ▼
   model.json ──┬──> oasis_core.py + Streamlit（手動で予測・購入判断）
                └──> autopilot.bundle.js（ブラウザで自動購入。model.json を焼き込み）
                └──> trainer.html（育成プランナー。model.json を焼き込み）
```

---

## 2. 渡すもの（この資料に同梱／取得先）

| もの | 場所 | 備考 |
|---|---|---|
| リポジトリ一式 | `footao/oasis-app` main `1d419bf` | public。clone すれば全部入る |
| 育成プランナー | `build_trainer.py` / `trainer_template.html` / `trainer.html` | **まだ未コミット**。この資料と一緒に渡す |
| 採取した生データ | `races.jsonl`（13MB, gitignore） | 自動採取 400+レース。timeline 付き。**モデル検証にはこれが要る** |
| 学習ログ | `logg/*.txt` | リポジトリに入っている。**学習が読むのはこれだけ** |
| 学習済みモデル | `model.json` | リポジトリに入っている |
| 公開中の育成プランナー | https://footao.github.io/status-calc/trainer.html | サブリポジトリ `footao/status-calc`（GitHub Pages） |
| リプレイ | `replay.html`（同上リポジトリで公開中） | timeline からレース再生 |

読む順: `REVIEW_BRIEF.md`（これ）→ `HANDOFF.md` → `REVIEW.md` →
`MODEL_REVIEW_20260901.md`（直近のモデル・データ点検）→ `ENGINE_NOTES.md`（エンジン逆算）。

> ⚠ `HANDOFF.md` は総合仕様として今も有効だが、**冒頭の版表記が 3.7.0 のまま古い**。
> 数値・版は本ファイルと `oasis_core.py` を正とすること。

---

## 3. アーキテクチャと不変条件（ここを壊すと静かに損をする）

1. **Python と JS は同じ計算をしていなければならない。**
   `oasis_core.py` ↔ `bookmarklets/src/model.js`。
   特徴量を1つ足して JS を忘れる事故が実際に起きるので `parity_test.py` がある
   （装備効果150件 / 単勝配分48件 / 3連単配分40件 / 予測360頭を突き合わせる）。
   **レビュー時は「Python側にしかない分岐」を探すのが一番当たりが多い。**
2. **JS 側に定数を持たせない。** clamp・σ・duty・プール仮定などはすべて `model.json` 経由。
   例外的に `bm.js` が clamp を持っていて事故った履歴あり（現在は同期済み・selftest P10 が見張る）。
3. **app と core は必ずセットで更新。** `oasis_app.py` の `REQUIRED_CORE` と
   `oasis_core.CORE_VERSION` が不一致なら起動を止める。
4. **`bookmarklets/src/` が本物。** 同名の `.js` がリポジトリ直下と `src/` にも残っている（残骸）。
   `build_autopilot.py` が読むのは `bookmarklets/src/` だけ。**未整理の負債。**
5. **配布物はビルド成果物。** `autopilot.bundle.js` / `trainer.html` / `replay.html` /
   `oasis_score_formula.html` は生成物。手で直すと次のビルドで消える
   （実際に一度、手修正が消えかけてソースへ移植し直した）。
6. **モデルは焼き込み。** オートパイロットと育成プランナーは `model.json` を埋め込む。
   **再学習したら両方ビルドし直す**（`build_autopilot.py` / `build_trainer.py`）。
7. jsDelivr のキャッシュは12時間。緊急時は
   `https://purge.jsdelivr.net/gh/footao/oasis-app@main/autopilot.bundle.js`。

---

## 4. モデルの中身（レビューの足がかり）

- **的**: `score = 100000 / finish_time` の log を**レース内で中心化**したもの。Ridge回帰。
  距離別に log項と線形項を持つハイブリッド特徴量＋パッシブのダミー＋距離との交互作用（縮小あり）。
- **σ**: `race_sigma 0.02155`（単勝）/ `tri_sigma 0.02538`（3連単）。
  **このセッションで 0.0127 から校正し直した**（旧値は3連単のEVを約1.4倍に過大評価していた）。
- **パリミュチュエル**: 単勝はΣ(1/odds)=1.0003 の実測（＝控除ほぼ無し）。
  3連単プールは `/api/trifecta/pool` から取得。**プール総額を仮定する箇所が残っている**（単勝20万）。
- **装備・お守り**: 効果の**大きさは説明文の正規表現**から、**発動範囲(scope/duty)は `model.json` の
  `item_scope`** から取る。説明文だけから推測すると『首位の呪い』が12倍に化ける（コメント参照）。
  scope は `always / conditional / phase / aptitude / variance / same_species / learned / lead`。
- **首位の呪い（scope=lead）の2段階近似**（このセッションで追加、要レビュー）:
  「先頭にいる間だけ効く」ので発動割合が馬ごとに桁違い。
  `先頭割合 = 0.655×P(1着) + 0.345×(1/頭数)`（実測相関 0.62）を使い、
  1回予測 → 1着確率を出す → duty を引き直して**もう一度予測**する
  （`lead_adjusted_base` / `leadAdjustedBase`）。スタミナ消費も ×1.04。
- **スタミナ収支**: 距離ごとに `clamp(c×基礎値, lo, hi) × 区間数` が必要量。
  余りは正の特徴量、不足は**必要量に対する割合**で罰（同じ「不足5」でも短距離と長距離で意味が違うため）。
  clamp の数値は timeline の実測から直読み（`STAMINA_COST_LAW`）。
- **学習から除外している期間**: 装備実装（08/17 17:25）〜ログに装備が載り始める（08/19 18:10）の
  11レース（`ITEM_LOG_GAP`）。除外でホールドアウト1着的中 81.4%→83.7%。

---

## 5. 確定済みの判断（実測して決着済み・蒸し返し不要）

| 判断 | 根拠 |
|---|---|
| **σを安全側に手で膨らませない** | 過去に「安全のため×1.25」を入れたら偽の+EVを量産して逆効果。撤回済み。資金保全は分数ケリー・λ・最小的中率フィルタが担う |
| **λ（モデル信頼度）は 1.0 のまま** | 実測の最適は 0.80 だがオーナー判断で据え置き。適応的な切替も見送り |
| **単勝に「最小オッズ下限」を入れない** | 提案したが、予測95%以上の本命は 41/41 で的中。入れると損なので撤回 |
| **オッズ1.5下限のロジックは仕様変更で不要**（2026/08/24, NPC単勝プール導入） | ゲーム側の仕様変更を確認済み。`diagnose_floor_odds` は現仕様でも安全 |
| **出走メンバー・ペース・位置取りは速度に影響しない** | オーナー明言＋除外テストで確認。例外は同族嫌悪パッシブのみ |
| **レース再現シミュレータへの移行は棚上げ** | 実測 rating+fatigue から再現すると1着97.8%だが、**事前予測**では78.7%で現行82.5%に負ける。`stamina_cost` にレース×馬ごとの乱数（±3〜5%、最大30%）が乗っているため。再開条件は「その乱数を timeline から特定できること」 |
| **`logg/` を実名のまま public 公開** | オーナー判断（2026/08/15）。匿名化したければ `anonymize_logg.py` |
| **オートパイロットは13秒前に解析を始める** | 8秒・10秒も試したが13秒に戻した。監視間隔は `LEAD_SEC` の1/5以下（selftest P19 が関係式で検査） |

---

## 6. このセッションで入れた変更（レビュー対象の中心）

1. **σの再校正** 0.0127 → 0.02155 / 0.02538。**今回いちばん金に効く変更**。
2. **単勝の口数上限バグ修正**: 残数を絶対上限として渡していて 99口までしか買えなかった
   （`winBetPicksPool` 内で既購入分を二重に引いていた）。Streamlit 側は正しく、オートパイロットだけの不具合。
3. **首位の呪いの位置依存 duty**（§4）＋ スタミナ消費 ×1.04。
4. **スタミナ clamp を timeline 実測値へ修正**（`bm.js` の重複定義も同期）。
5. **装備ログ欠落期間を学習から除外**（`ITEM_LOG_GAP`）。
6. **購入まとめの出力**（購入した組・口数・的中率・購入前/実効オッズ・EV・見送った候補）と
   まとめだけコピーするボタン。Discordが `14:59:49` を絵文字と誤認するので `14時59分` 表記。
7. **`ENGINE_NOTES.md`**: timeline 39,670区間からレースエンジンを逆算
   （`v = k×rating^0.8709×fatigue^0.9136`、残差1.24%＝ゲーム乱数の下限とほぼ同じ）。
8. **育成プランナー `trainer.html`**（新規・未コミット）。特訓30回の配分496通りを全評価し、
   基準10頭に混ぜたモンテカルロで**平均着順**を出して距離を推奨する。
   「1回の伸び」は手入力（既定3＝1〜5の期待値）。

---

## 7. 未解決・怪しいと思っている所（重点的に見てほしい）

- [ ] **`bm.js` / `model.js` / `autopilot.js` に Python 側と食い違う分岐が残っていないか**（最重要）。
- [ ] **`lead` scope の2段階近似**: 相関0.62の線形近似を予測に組み込むのは妥当か。
      3連単の組確率への波及（1頭の base が動くと全組が動く）を過小評価していないか。
- [ ] **σの校正方法そのもの**: 何を残差と見て推定したか、3連単用に別σを持つ設計は正当か。
- [ ] **プール総額の仮定**（単勝20万など）が残る箇所の影響範囲。
- [ ] **3連単の本命1点が予測を下回っている**（予測44.9% vs 実測31.2%, `MODEL_REVIEW_20260901.md`）。
      σではなく特徴量の問題ではないか。サンプル数不足の可能性もある。
- [ ] **学習は `logg/` のテキストしか読まない**。自動採取した `races.jsonl`（timeline付き・より新しい）が
      学習に使われていない。二重管理をやめるべきか。
- [ ] **オートパイロットの失敗時挙動**: 通信失敗・オッズ未成立・締切直前の再試行で二重購入が起きないか。
- [ ] **育成プランナーの基準フィールド**: 「直近の出走馬を強さ順に並べて分位から10頭」という選び方が
      距離間の比較として妥当か（現役の完成馬が混ざるため、育て終わった直後の馬は1着確率0%になる）。
- [ ] **同じ .js が3箇所にある**（§3-4）。消して良いはずだが未確認。
- [ ] `HANDOFF.md` 冒頭の版表記が古い。

---

## 8. 検証の走らせ方

```bash
cd C:\oasissfable\repo
python selftest.py logg      # ログ解析→学習→予測→ベットログ→回帰テスト P1〜P23
python parity_test.py        # Python↔JS の一致（node が要る）
python evaluate.py races.jsonl   # 実測重み・スタミナ罰則を train/test で判定
python build_autopilot.py    # autopilot.bundle.js を再生成
python build_trainer.py      # trainer.html を再生成（model.json 更新後は必須）
python make_replay.py        # replay.html を再生成
```

- `selftest.py` は**回帰テストの本体**。仕様を1つ変えるたびにここへ検査を足してきた
  （P19=オートパイロットのタイミングと購入まとめ、P22=lead duty、P23=clamp 値、等）。
- 育成プランナーは HTML 内に自己チェックを持つ（配分の列挙数・合計・単調性・基準フィールドの頭数）。
  壊れていれば画面に赤字で出る。
- `harvest_daily.bat` が毎日 `races.jsonl` を更新している。

---

## 9. 環境・作業上の注意

- 開発はクラウドセッション（Linuxコンテナ）からユーザーPCへブリッジして行っている。
  **`device_bash` が無いため、git・ビルド・実行はオーナーが手元で回している。**
  ファイルはブリッジで直接書き込む（配信しただけで適用済みと思い込んで事故った履歴あり）。
- `api.oasis.red` は**サーバからも到達可能**。結果APIは token 不要、購入系だけ token が要る。
- ログ・URLに**購入トークンを混ぜないこと**（public リポジトリ）。
- 管理系エンドポイントは 403。探索しない。

---

## 10. 用語

| 語 | 意味 |
|---|---|
| 口 | 賭けの単位。3連単1口=10,000rrc（合計20口まで）/ 単勝1口=1,000rrc（合計100口まで） |
| duty | パッシブ・装備が「レース中どれだけの割合で効いているか」。0〜1 |
| base | レース内で中心化した予測スコア。モンテカルロの平均値として使う |
| λ | 市場情報が無い組に掛けるモデル確率の割引 |
| timeline | result API が返す100mごとの記録（残スタミナ・消費・疲労補正・rating・順位） |
| rrc | ゲーム内通貨 |
