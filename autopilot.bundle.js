/* おあしすっち オートパイロット バンドル（モデル埋め込み・自動生成）*/
/* 学習 110レース  2026/07/28〜2026/08/15 */
(()=>{window.__OASIS_MODEL={"core_version":"3.7.0","trained_at":"2026-08-16T12:35:01","n_races":110,"date_min":"2026/07/28","date_max":"2026/08/15","race_spearman":0.9409055694750882,"coef":{"短距離:切片":-0.0109964565,"短距離:log(SP)":0.3244578166,"短距離:log(PW)":0.0571855375,"短距離:log(ST)":-0.0623038388,"短距離:lin(SP)":0.2537103634,"短距離:lin(PW)":0.0877653746,"短距離:lin(ST)":-0.0099186895,"マイル:切片":0.0051373924,"マイル:log(SP)":0.2274136445,"マイル:log(PW)":0.1259155758,"マイル:log(ST)":0.0180095703,"マイル:lin(SP)":0.2352801327,"マイル:lin(PW)":0.1553810253,"マイル:lin(ST)":-0.0129143617,"中距離:切片":0.003405107,"中距離:log(SP)":0.1218477311,"中距離:log(PW)":0.1773970997,"中距離:log(ST)":0.1103374781,"中距離:lin(SP)":0.231993672,"中距離:lin(PW)":0.1882437728,"中距離:lin(ST)":0.0570160002,"長距離:切片":0.0024539572,"長距離:log(SP)":0.1249337755,"長距離:log(PW)":0.1600712443,"長距離:log(ST)":0.1469411375,"長距離:lin(SP)":0.1301556065,"長距離:lin(PW)":0.1441301593,"長距離:lin(ST)":0.0939325316,"スタミナ余り":0.0247178055,"スタミナ不足":-0.0588671531,"好調":-6.19886e-05,"不調":0.0013560866,"粘り腰":0.0238867108,"粘り腰×短距離":-0.0030133687,"粘り腰×マイル":-0.0028833379,"粘り腰×中距離":0.0142236611,"粘り腰×長距離":0.0036164009},"intercept":-2.284326546824009,"race_sigma":0.00448,"tri_sigma":0.01421,"spec":{"スピードスター":{"mult":{"speed":1.35,"stamina":0.9},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"脳筋":{"mult":{"power":1.35,"speed":0.9},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"マイペース":{"mult":{"stamina":1.35,"power":0.9},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"器用貧乏":{"mult":{"speed":1.05,"power":1.05,"stamina":1.05},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"スピード大アップ":{"mult":{"speed":1.25},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"パワー大アップ":{"mult":{"power":1.25},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"スタミナ大アップ":{"mult":{"stamina":1.25},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"スピード小アップ":{"mult":{"speed":1.15},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"パワー小アップ":{"mult":{"power":1.15},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"スタミナ小アップ":{"mult":{"stamina":1.15},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"勝負師":{"mult":{"speed":1.25,"power":1.25,"stamina":1.25},"scope":"conditional","scope_arg":null,"duty":0.05,"sigma_mult":1.0},"同族嫌悪":{"mult":{"speed":1.2,"power":1.2,"stamina":1.2},"scope":"same_species","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"芝得意":{"mult":{"speed":1.1,"power":1.1,"stamina":1.1},"scope":"aptitude","scope_arg":"芝","duty":1.0,"sigma_mult":1.0},"ダート得意":{"mult":{"speed":1.1,"power":1.1,"stamina":1.1},"scope":"aptitude","scope_arg":"ダート","duty":1.0,"sigma_mult":1.0},"短距離得意":{"mult":{"speed":1.15,"power":1.15,"stamina":1.15},"scope":"aptitude","scope_arg":"短距離","duty":1.0,"sigma_mult":1.0},"マイル得意":{"mult":{"speed":1.15,"power":1.15,"stamina":1.15},"scope":"aptitude","scope_arg":"マイル","duty":1.0,"sigma_mult":1.0},"中距離得意":{"mult":{"speed":1.15,"power":1.15,"stamina":1.15},"scope":"aptitude","scope_arg":"中距離","duty":1.0,"sigma_mult":1.0},"長距離得意":{"mult":{"speed":1.15,"power":1.15,"stamina":1.15},"scope":"aptitude","scope_arg":"長距離","duty":1.0,"sigma_mult":1.0},"ロケットスタート":{"mult":{"speed":1.12},"scope":"phase","scope_arg":"序盤","duty":0.3333333333333333,"sigma_mult":1.0},"中盤加速":{"mult":{"power":1.1},"scope":"phase","scope_arg":"中盤","duty":0.3333333333333333,"sigma_mult":1.0},"末脚":{"mult":{"power":1.12},"scope":"phase","scope_arg":"終盤","duty":0.3333333333333333,"sigma_mult":1.0},"二の脚":{"mult":{"speed":1.08},"scope":"phase","scope_arg":"中盤","duty":0.15,"sigma_mult":1.0},"ロングスパート":{"mult":{"power":1.07,"stamina":0.92},"scope":"phase","scope_arg":"終盤","duty":0.5,"sigma_mult":1.0},"ペース配分":{"mult":{"speed":0.95,"stamina":1.15},"scope":"phase","scope_arg":"序盤","duty":0.3333333333333333,"sigma_mult":1.0},"逃げの心得":{"mult":{"speed":1.08,"power":1.08,"stamina":0.9504},"scope":"phase","scope_arg":"序盤","duty":0.3333333333333333,"sigma_mult":1.0},"差しの構え":{"mult":{"power":1.08},"scope":"conditional","scope_arg":"中盤","duty":0.16666666666666666,"sigma_mult":1.0},"追い込み":{"mult":{"power":1.12},"scope":"conditional","scope_arg":"終盤","duty":0.16666666666666666,"sigma_mult":1.0},"競り合い":{"mult":{"power":1.06},"scope":"conditional","scope_arg":null,"duty":0.5,"sigma_mult":1.0},"独走態勢":{"mult":{"stamina":1.06},"scope":"conditional","scope_arg":null,"duty":0.2,"sigma_mult":1.0},"不屈":{"mult":{"power":1.08},"scope":"conditional","scope_arg":null,"duty":0.1,"sigma_mult":1.0},"緊急回復":{"mult":{"stamina":1.06},"scope":"conditional","scope_arg":null,"duty":0.5,"sigma_mult":1.0},"セカンドウインド":{"mult":{"stamina":1.08},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"省エネ走法":{"mult":{"stamina":1.08},"scope":"always","scope_arg":null,"duty":1.0,"sigma_mult":1.0},"安定感":{"mult":{},"scope":"variance","scope_arg":null,"duty":1.0,"sigma_mult":0.5},"粘り腰":{"mult":{},"scope":"learned","scope_arg":null,"duty":1.0,"sigma_mult":1.0}},"unspecced":["粘り腰"],"catalog":{"スピードスター":"stat","脳筋":"stat","マイペース":"stat","勝負師":"stat","器用貧乏":"stat","同族嫌悪":"stat","スピード大アップ":"stat","パワー大アップ":"stat","スタミナ大アップ":"stat","スピード小アップ":"stat","パワー小アップ":"stat","スタミナ小アップ":"stat","芝得意":"aptitude","ダート得意":"aptitude","短距離得意":"aptitude","マイル得意":"aptitude","中距離得意":"aptitude","長距離得意":"aptitude","ロケットスタート":"phase","二の脚":"phase","独走態勢":"phase","不屈":"phase","ロングスパート":"phase","追い込み":"phase","中盤加速":"phase","競り合い":"phase","省エネ走法":"phase","差しの構え":"phase","緊急回復":"phase","安定感":"phase","粘り腰":"phase","セカンドウインド":"phase","逃げの心得":"phase","ペース配分":"phase","末脚":"phase"},"code_map":{"speed_star":"スピードスター","muscle_head":"脳筋","steady_runner":"マイペース","jack_of_all":"器用貧乏","speed_l":"スピード大アップ","power_l":"パワー大アップ","stamina_l":"スタミナ大アップ","speed_s":"スピード小アップ","power_s":"パワー小アップ","stamina_s":"スタミナ小アップ","gambler":"勝負師","same_kind_boost":"同族嫌悪","turf_specialist":"芝得意","dirt_specialist":"ダート得意","short_special":"短距離得意","mile_special":"マイル得意","middle_special":"中距離得意","long_special":"長距離得意","rocket_start":"ロケットスタート","mid_acceleration":"中盤加速","final_kick":"末脚","second_gear":"二の脚","long_spurt":"ロングスパート","pace_control":"ペース配分","front_runner":"逃げの心得","closer_stance":"差しの構え","deep_closer":"追い込み","duel_spirit":"競り合い","solo_lead":"独走態勢","indomitable":"不屈","emergency_recovery":"緊急回復","second_wind":"セカンドウインド","energy_saver":"省エネ走法","consistency":"安定感","tenacious":"粘り腰"},"aptitude_match":{"芝得意":["track","芝"],"ダート得意":["track","ダート"],"短距離得意":["dist","短距離"],"マイル得意":["dist","マイル"],"中距離得意":["dist","中距離"],"長距離得意":["dist","長距離"]},"dist_list":["短距離","マイル","中距離","長距離"],"track_list":["芝","ダート"],"variance_share":0.8,"interaction_shrink":0.5,"stamina_cost_law":{"短距離":{"c":0.01335,"lo":1.955,"hi":2.967,"n_seg":10},"マイル":{"c":0.01976,"lo":2.234,"hi":3.067,"n_seg":15},"中距離":{"c":0.02999,"lo":2.541,"hi":3.737,"n_seg":20},"長距離":{"c":0.04077,"lo":2.57,"hi":3.68,"n_seg":25}},"phase_early":[0.6,0.3,0.1],"dist_balance":{"短距離":[1.4,0.8,0.5],"マイル":[1.0,1.0,1.0],"中距離":[0.9,1.3,1.3],"長距離":[0.6,1.0,1.4]},"odds_floor":1.5,"stake_unit":10000,"trifecta_pool_seed":200000,"max_total_units":20,"min_field_trifecta":8};
const OasisModel = (() => { function effectiveStats(speed, power, stamina, passives, dist, track, M, ctx) { ctx = ctx || {}; const v = { speed: +speed, power: +power, stamina: +stamina }; for (const p of (passives || [])) { const sp = M.spec[p]; if (!sp || !sp.mult || !Object.keys(sp.mult).length) continue; if (sp.scope === 'aptitude' && sp.scope_arg !== dist && sp.scope_arg !== track) continue; if (sp.scope === 'same_species' && !ctx.same_species) continue; const duty = Math.min(Math.max(sp.duty == null ? 1 : sp.duty, 0), 1); for (const k of Object.keys(sp.mult)) { if (k in v) v[k] *= (1 + (+sp.mult[k] - 1) * duty); } } for (const k of Object.keys(v)) v[k] = Math.max(v[k], 1); return v; } function sigmaMultiplier(passives, M) { let m = 1; for (const p of (passives || [])) { const sp = M.spec[p]; if (sp) m *= (sp.sigma_mult == null ? 1 : +sp.sigma_mult); } if (m === 1) return 1; const f = Math.min(Math.max(M.variance_share, 0), 1); return Math.sqrt(f * m * m + (1 - f)); } function staminaBudget(e, dist, M) { const L = (M.stamina_cost_law || {})[dist]; if (!L) return [0, 0, 0]; const w = M.phase_early, b = (M.dist_balance || {})[dist] || [1, 1, 1]; const base = e.speed * w[0] * b[0] + e.power * w[1] * b[1] + e.stamina * w[2] * b[2]; const need = Math.min(Math.max(L.c * base, L.lo), L.hi) * L.n_seg; const have = Math.floor(e.stamina); return [need, Math.max(0, need - have), Math.max(0, have - need)]; } function rowFeatures(h, dist, track, M, ctx) { const e = effectiveStats(h.speed, h.power, h.stamina, h.passives, dist, track, M, ctx); const lg = { SP: Math.log(Math.max(e.speed, 1)), PW: Math.log(Math.max(e.power, 1)), ST: Math.log(Math.max(e.stamina, 1)) }; const ln = { SP: e.speed / 100, PW: e.power / 100, ST: e.stamina / 100 }; const f = {}; for (const d of M.dist_list) { const m = (dist === d) ? 1 : 0; f[`${d}:切片`] = m; for (const s of ['SP', 'PW', 'ST']) { f[`${d}:log(${s})`] = m * lg[s]; f[`${d}:lin(${s})`] = m * ln[s]; } } const bud = staminaBudget(e, dist, M); f['スタミナ余り'] = bud[2] / 10; f['スタミナ不足'] = bud[1] / 10; const cond = h.condition || '普通'; f['好調'] = cond === '好調' ? 1 : 0; f['不調'] = cond === '不調' ? 1 : 0; const pset = new Set(h.passives || []); for (const p of M.unspecced) { const has = pset.has(p) ? 1 : 0; if (M.catalog[p] === 'aptitude') { const [col, val] = M.aptitude_match[p]; const ok = (col === 'track') ? (track === val) : (dist === val); f[p] = ok ? has : 0; } else { f[p] = has; for (const d of M.dist_list) { f[`${p}×${d}`] = M.interaction_shrink * has * ((dist === d) ? 1 : 0); } } } return f; } function sameSpeciesFlags(horses) { const key = h => String(h.species || String(h.name || '').replace(/\s*#\s*\d+\s*$/, '')).trim(); const cnt = {}; horses.forEach(h => { const k = key(h); cnt[k] = (cnt[k] || 0) + 1; }); return horses.map(h => cnt[key(h)] >= 2); } function predictBase(horses, dist, track, M) { const same = sameSpeciesFlags(horses); const raw = horses.map((h, i) => { const f = rowFeatures(h, dist, track, M, { same_species: same[i] }); let s = M.intercept; for (const k of Object.keys(f)) { const c = M.coef[k]; if (c !== undefined) s += c * f[k]; } return s; }); const mean = raw.reduce((a, b) => a + b, 0) / raw.length; return raw.map(x => x - mean); } function makeRng(seed) { let s = seed >>> 0; const u32 = () => { s = (s + 0x6D2B79F5) >>> 0; let t = s; t = Math.imul(t ^ (t >>> 15), t | 1); t ^= t + Math.imul(t ^ (t >>> 7), t | 61); return ((t ^ (t >>> 14)) >>> 0) / 4294967296; }; let spare = null; return () => { if (spare !== null) { const v = spare; spare = null; return v; } let u = 0, v = 0, sq = 0; do { u = u32() * 2 - 1; v = u32() * 2 - 1; sq = u * u + v * v; } while (sq >= 1 || sq === 0); const m = Math.sqrt(-2 * Math.log(sq) / sq); spare = v * m; return u * m; }; } function simulateTrifecta(base, sigmas, nSim, seed) { const n = base.length; const randn = makeRng(seed == null ? 42 : seed); const win = new Float64Array(n); const combo = new Map(); const val = new Float64Array(n); for (let it = 0; it < nSim; it++) { for (let i = 0; i < n; i++) val[i] = base[i] + randn() * sigmas[i]; let a = -1, b = -1, c = -1; for (let i = 0; i < n; i++) { const x = val[i]; if (a < 0 || x > val[a]) { c = b; b = a; a = i; } else if (b < 0 || x > val[b]) { c = b; b = i; } else if (c < 0 || x > val[c]) { c = i; } } win[a]++; if (n >= 3) { const k = (a * n + b) * n + c; combo.set(k, (combo.get(k) || 0) + 1); } } const winP = Array.from(win, x => x / nSim); const comboP = []; for (const [k, v] of combo) { comboP.push({ i: Math.floor(k / (n * n)), j: Math.floor(k / n) % n, k: k % n, p: v / nSim }); } comboP.sort((x, y) => y.p - x.p); return { win: winP, combo: comboP }; } function horseSigmas(horses, sigma, M) { return horses.map(h => sigma * sigmaMultiplier(h.passives, M)); } return { effectiveStats, sigmaMultiplier, rowFeatures, predictBase, sameSpeciesFlags, simulateTrifecta, horseSigmas, makeRng }; })(); if (typeof module !== 'undefined' && module.exports) module.exports = OasisModel;
(async () => { 'use strict'; try { const prev = document.getElementById('oasis-autopilot-panel'); if (prev) prev.remove(); if (window.__oasisAutopilotTimer) clearInterval(window.__oasisAutopilotTimer); } catch (e) {} const CFG = { RACE_HOURS: [9, 12, 15, 18, 21, 23], RACE_MINUTE: 0, LEAD_SEC: 120, WINDOW_SEC: 900, MAX_UNITS_PER_RACE: 3, UNITS_PER_COMBO: 1, EDGE_MIN: 0.10, MODEL_WEIGHT: 0.7, MIN_PROB: 0.02, ODDS_TOP_N: 40, API_BASES: ['https://api.oasis.red', null], IMMEDIATE: true, N_SIM: 200000, DAILY_BUDGET: 200000, MAX_SANE_EDGE: 3.0, MAX_SANE_ODDS: 5000, MIN_POOL: 300000, MIN_TRAIN_RACES: 20, MODEL_URL: 'https://raw.githubusercontent.com/footao/oasis-app/main/model.json', MODEL_JSON: null, }; let API = null; const LS = 'oasis_autopilot_v1', LSA = 'oasis_autopilot_auth', LSB = 'oasis_autopilot_api'; const esc = s => String(s).replace(/[&<>"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])); const sleep = ms => new Promise(r => setTimeout(r, ms)); const jget = async u => { try { const r = await fetch(u); if (!r.ok) return null; return await r.json(); } catch (e) { return null; } }; const today = () => new Date().toLocaleDateString('sv-SE'); function authFromUrl(str) { try { const u = new URL(str, location.href); const p = u.searchParams; const a = { guild: p.get('guild'), user: p.get('user'), token: p.get('token'), sid: parseInt(p.get('race') || p.get('schedule_id'), 10) }; return (a.guild && a.token) ? a : null; } catch (e) { return null; } } function loadAuth() { const fromUrl = authFromUrl(location.href); if (fromUrl) { localStorage.setItem(LSA, JSON.stringify(fromUrl)); return fromUrl; } try { return JSON.parse(localStorage.getItem(LSA) || 'null'); } catch (e) { return null; } } let AUTH = loadAuth(); function loadState() { let s = {}; try { s = JSON.parse(localStorage.getItem(LS) || '{}'); } catch (e) {} if (s.day !== today()) s = { day: today(), spent: 0, done: {}, log: [] }; s.done = s.done || {}; s.log = s.log || []; s.spent = s.spent || 0; return s; } const saveState = s => { try { localStorage.setItem(LS, JSON.stringify(s)); } catch (e) {} }; let ST = loadState(); let PENDING = null; const ov = document.createElement('div'); ov.id = 'oasis-autopilot-panel'; ov.style.cssText = 'position:fixed;z-index:99999;right:8px;left:auto;width:470px;' + 'max-width:calc(100vw - 16px);max-height:82vh;overflow-y:auto;' + 'bottom:calc(12px + env(safe-area-inset-bottom,0px));' + 'background:#12121f;border:2px solid #e2b96f;border-radius:10px;padding:.8rem;color:#eee;' + 'font-family:sans-serif;font-size:.85rem;box-shadow:0 6px 20px rgba(0,0,0,.5);' + '-webkit-overflow-scrolling:touch'; if (window.matchMedia && window.matchMedia('(max-width:640px)').matches) { ov.style.left = '8px'; ov.style.width = 'auto'; } ov.innerHTML = '<b style="color:#e2b96f">🛩 3連単 オートパイロット</b>' + '<span id=_auth style="float:right;font-size:.72rem"></span>' + '<div id=_stat style="margin:.45rem 0;font-size:.76rem;line-height:1.6"></div>' + '<div id=_pick style="display:none;margin:.5rem 0;padding:.5rem;background:#0d1a0d;' + 'border:1px solid #2e7d32;border-radius:6px;font-size:.76rem"></div>' + '<details style="margin:.4rem 0"><summary style="cursor:pointer;font-size:.74rem;color:#aaa">' + '🔑 購入リンクを貼り直す（token が変わる場合）</summary>' + '<textarea id=_link placeholder="BOTが発行した購入リンクを貼り付け" ' + 'style="width:100%;height:46px;margin-top:.35rem;background:#0b0b14;color:#e2b96f;' + 'border:1px solid #444;border-radius:5px;padding:.35rem;font-size:.7rem"></textarea>' + '<button id=_save style="margin-top:.3rem;padding:.3rem .7rem;background:#e2b96f;color:#12121f;' + 'border:none;border-radius:5px;cursor:pointer;font-weight:700">保存</button></details>' + '<div id=_log style="max-height:200px;overflow-y:auto;font-size:.72rem;line-height:1.5;' + 'background:#0b0b14;border:1px solid #333;border-radius:6px;padding:.4rem"></div>' + '<div style="display:flex;gap:.4rem;margin-top:.5rem">' + '<button id=_now style="flex:1;padding:.6rem;background:#2e7d32;color:#fff;border:none;border-radius:5px;cursor:pointer">今すぐ解析</button>' + '<button id=_clr style="padding:.6rem .7rem;background:#444;color:#fff;border:none;border-radius:5px;cursor:pointer">ログ消去</button>' + '<button id=_x style="padding:.6rem .7rem;background:#7a2222;color:#fff;border:none;border-radius:5px;cursor:pointer">停止</button></div>'; document.body.appendChild(ov); const $ = i => document.getElementById(i); function log(m, c) { ST.log.unshift({ m: `[${new Date().toLocaleTimeString('ja-JP')}] ${m}`, c: c || '' }); ST.log = ST.log.slice(0, 300); saveState(ST); render(); } function render() { const next = nextRaceTime(); const left = Math.max(0, Math.round((next - Date.now()) / 1000)); $('_auth').innerHTML = AUTH ? `<span style="color:#81c784">🔑 ${esc(String(AUTH.user || '').slice(0, 8))}…</span>` : '<span style="color:#ef5350">🔑 未設定</span>'; $('_stat').innerHTML = `次のレース <b>${next.toLocaleTimeString('ja-JP', {hour:'2-digit', minute:'2-digit'})}</b>` + `（あと ${Math.floor(left/60)}分${String(left%60).padStart(2,'0')}秒）<br>` + `本日 <b>${ST.spent.toLocaleString()}</b>/${CFG.DAILY_BUDGET.toLocaleString()} rrc` + ` 解析済み ${Object.keys(ST.done).length} レース`; $('_log').innerHTML = ST.log.map(x => `<span style="color:${x.c || '#aaa'}">${esc(x.m)}</span>`).join('<br>') || '<span style="color:#666">（待機中）</span>'; } function nextRaceTime() { const now = new Date(); for (let d = 0; d < 2; d++) for (const h of CFG.RACE_HOURS) { const t = new Date(now); t.setDate(now.getDate() + d); t.setHours(h, CFG.RACE_MINUTE, 0, 0); if (t > now) return t; } return new Date(now.getTime() + 3600000); } async function resolveApi() { const saved = localStorage.getItem(LSB); const cands = []; if (saved) cands.push(saved); for (const b of CFG.API_BASES) { const u = (b === null) ? location.origin : b; if (!cands.includes(u)) cands.push(u); } for (const b of cands) { const r = await jget(`${b}/api/race/by-id/${AUTH.guild}/${AUTH.sid}?user=${AUTH.user}`); if (r && Array.isArray(r.pets)) { API = b; localStorage.setItem(LSB, b); log(`API: ${esc(b)}`, '#81c784'); return b; } } throw new Error('APIに到達できません（レースページで実行しているか確認してください）'); } let M = null; async function loadModel() { M = CFG.MODEL_JSON || window.__OASIS_MODEL || await jget(CFG.MODEL_URL); if (!M || !M.coef || !M.spec) throw new Error('model.json を読めません（MODEL_URL を確認）'); if (M.placeholder) { throw new Error('model.json が雛形のままです。build_autopilot.py で再生成してください'); } if (!(M.n_races >= CFG.MIN_TRAIN_RACES)) { throw new Error(`学習レースが ${M.n_races} 件しかありません` + `（${CFG.MIN_TRAIN_RACES}件以上必要）。model.json を作り直してください`); } log(`モデル読込 v${M.core_version} / 学習${M.n_races}レース` + ` / σ3連単 ${Number(M.tri_sigma).toFixed(4)}`, '#81c784'); } async function findRace() { const from = AUTH.sid || 0; for (let d = 0; d <= 10; d++) { const info = await jget(`${API}/api/race/by-id/${AUTH.guild}/${from + d}?user=${AUTH.user}`); if (info && Array.isArray(info.pets) && info.pets.length && info.phase === 'betting') { return { sid: from + d, info }; } } return null; } async function fetchOdds(sid, pets, combo) { const out = new Map(), want = combo.slice(0, CFG.ODDS_TOP_N); for (let i = 0; i < want.length; i += 10) { const got = await Promise.all(want.slice(i, i + 10).map(async c => { const d = await jget(`${API}/api/trifecta/odds?guild=${AUTH.guild}&schedule_id=${sid}` + `&first=${pets[c.i].pet_id}&second=${pets[c.j].pet_id}&third=${pets[c.k].pet_id}`); return { c, od: (d && typeof d.odds === 'number') ? d.odds : null }; })); got.forEach(g => { if (g.od) out.set(`${g.c.i}-${g.c.j}-${g.c.k}`, g.od); }); await sleep(120); } return out; } async function analyseRace(sid, info) { const pets = info.pets, n = pets.length; if (n < (M.min_field_trifecta || 8)) { log(`R${sid}: ${n}頭 → 3連単なし`, '#888'); return null; } const dist = info.distance || '', track = info.surface || ''; if (!M.dist_list.includes(dist)) { log(`R${sid}: 距離「${esc(dist)}」が未知 → 見送り`, '#ffb74d'); return null; } const horses = pets.map(h => ({ name: h.display_name || h.name, species: h.adult_key || null, speed: h.speed, power: h.power, stamina: h.stamina, condition: h.condition_label || '普通', passives: [h.passive_skill, h.passive_skill_2] .map(c => (c && c !== 'none') ? (M.code_map[c] || null) : null).filter(Boolean), })); const base = OasisModel.predictBase(horses, dist, track, M); const sig = OasisModel.horseSigmas(horses, M.tri_sigma, M); const { combo } = OasisModel.simulateTrifecta(base, sig, CFG.N_SIM, 42); const pool = ((await jget(`${API}/api/trifecta/pool?guild=${AUTH.guild}&schedule_id=${sid}`)) || {}).pool || 0; if (pool < CFG.MIN_POOL) { log(`R${sid}: プール ${pool.toLocaleString()} rrc は小さすぎ → 見送り`, '#888'); return null; } let odds = await fetchOdds(sid, pets, combo); if (!odds.size) { log(`R${sid}: オッズ取得失敗 → 見送り`, '#ffb74d'); return null; } const SEED = (M.trifecta_pool_seed == null ? 200000 : M.trifecta_pool_seed); if (pool > SEED) { const f = pool / (pool - SEED); const fixed = new Map(); for (const [k, v] of odds) fixed.set(k, v * f); odds = fixed; log(`R${sid}: オッズを x${f.toFixed(3)} 補正（初期プール金 ${SEED.toLocaleString()} rrc）`, '#888'); } let inv = 0; for (const od of odds.values()) inv += 1 / od; const norm = Math.max(inv, 1e-9), U_ = M.stake_unit || 10000; const picks = []; for (const c of combo.slice(0, CFG.ODDS_TOP_N)) { const od = odds.get(`${c.i}-${c.j}-${c.k}`); if (!od || od <= 1 || c.p < CFG.MIN_PROB) continue; const pBet = CFG.MODEL_WEIGHT * c.p + (1 - CFG.MODEL_WEIGHT) * ((1 / od) / norm); const eff = (pool + U_) / (pool / od + U_); const edge = pBet * eff - 1; if (eff > CFG.MAX_SANE_ODDS) continue; if (edge > CFG.MAX_SANE_EDGE) { log(`R${sid}: エッジ +${(edge*100).toFixed(0)}% は異常 → このレースは中止`, '#ef5350'); return null; } if (edge < CFG.EDGE_MIN) continue; picks.push({ c, od, eff, edge, p: c.p, names: [pets[c.i], pets[c.j], pets[c.k]].map(h => h.display_name || h.name) }); } if (!picks.length) { log(`R${sid}: エッジ${(CFG.EDGE_MIN*100)|0}%以上の点なし → 見送り`, '#888'); return null; } picks.sort((a, b) => b.edge - a.edge); const chosen = picks.slice(0, CFG.MAX_UNITS_PER_RACE); const cost = chosen.length * CFG.UNITS_PER_COMBO * U_; if (ST.spent + cost > CFG.DAILY_BUDGET) { log(`R${sid}: 本日の予算上限 → 見送り`, '#ffb74d'); return null; } return { sid, pets, picks: chosen, cost, unit: U_ }; } function showPending(pl) { PENDING = pl; $('_pick').style.display = 'block'; $('_pick').innerHTML = `<b style="color:#81c784">R${pl.sid} 推奨 ${pl.picks.length}点 / ${pl.cost.toLocaleString()} rrc</b><br>` + pl.picks.map(p => ` ${esc(p.names.join(' → '))} 実効od ${p.eff.toFixed(1)} ` + `<span style="color:#81c784">+${(p.edge*100).toFixed(0)}%</span>`).join('<br>') + '<br><button id=_buy style="width:100%;margin-top:.5rem;padding:.7rem;background:#2e7d32;' + 'color:#fff;border:none;border-radius:5px;font-weight:700;cursor:pointer">🛒 これを購入する</button>'; $('_buy').onclick = doBuy; try { if (Notification && Notification.permission === 'granted') { new Notification('おあしすっち 買い目を用意しました', { body: `R${pl.sid} ${pl.picks.length}点 / ${pl.cost.toLocaleString()} rrc` }); } } catch (e) {} } let buying = false; async function doBuy() { if (buying || !PENDING) return; buying = true; const btn = $('_buy'); if (btn) { btn.disabled = true; btn.style.opacity = .5; btn.textContent = '購入中…'; } const pl = PENDING; for (const pk of pl.picks) { const label = pk.names.join('→'); try { const r = await fetch(`${API}/api/trifecta/buy`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ user: AUTH.user, guild: AUTH.guild, race: pl.sid, first: pl.pets[pk.c.i].pet_id, second: pl.pets[pk.c.j].pet_id, third: pl.pets[pk.c.k].pet_id, amount: CFG.UNITS_PER_COMBO * pl.unit, token: AUTH.token }) }); let d = {}; try { d = await r.json(); } catch (e) {} if (r.ok && d.status !== 'error') { ST.spent += CFG.UNITS_PER_COMBO * pl.unit; log(`R${pl.sid} ✅ ${label} +${(pk.edge*100).toFixed(0)}%`, '#81c784'); } else { const msg = String(d.detail || d.message || ('HTTP ' + r.status)); log(`R${pl.sid} ❌ ${label} ${esc(msg)}`, '#ef5350'); if (/token|auth|unauthor|expire/i.test(msg)) { log('→ token が無効のようです。新しい購入リンクを貼り直してください。', '#ffb74d'); } } } catch (e) { log(`R${pl.sid} ⚠ ${label} 通信エラー（送信済みか不明）: ${esc(e.message)}`, '#ffb74d'); } await sleep(400); } ST.done[pl.sid] = { t: Date.now(), n: pl.picks.length }; saveState(ST); PENDING = null; $('_pick').style.display = 'none'; buying = false; render(); } let busy = false, stopped = false, errors = 0; async function tick(force) { if (stopped || busy || PENDING) { render(); return; } if (!AUTH) { render(); return; } const left = (nextRaceTime() - Date.now()) / 1000; if (!force && (left > CFG.WINDOW_SEC || left > CFG.LEAD_SEC)) { render(); return; } busy = true; try { const r = await findRace(); if (!r) { if (force) log('受付中のレースが見つかりません（締切済みかもしれません）', '#888'); } else if (ST.done[r.sid] && !force) { /* 済み */ } else { const pl = await analyseRace(r.sid, r.info); if (pl) showPending(pl); else ST.done[r.sid] = { t: Date.now(), n: 0 }; saveState(ST); } errors = 0; } catch (e) { errors++; log(`エラー(${errors}/3): ${esc(e.message)}`, '#ef5350'); if (errors >= 3) { stopped = true; log('連続エラーのため停止しました', '#ef5350'); } } finally { busy = false; render(); } } $('_save').onclick = () => { const a = authFromUrl($('_link').value.trim()); if (!a) { log('リンクを読めません（guild と token を含むURLを貼ってください）', '#ef5350'); return; } AUTH = a; localStorage.setItem(LSA, JSON.stringify(a)); $('_link').value = ''; log('購入リンクを更新しました', '#81c784'); }; $('_x').onclick = () => { stopped = true; log('停止しました', '#ffb74d'); }; $('_clr').onclick = () => { ST.log = []; saveState(ST); render(); }; $('_now').onclick = () => { log('手動で解析します', '#e2b96f'); tick(true); }; try { try { if (Notification && Notification.permission === 'default') Notification.requestPermission(); } catch (e) {} await loadModel(); if (!AUTH) { log('購入リンクが未設定です。BOTが出したリンクを開いてから実行してください。', '#ffb74d'); render(); return; } await resolveApi(); render(); if (CFG.IMMEDIATE) { log('受付中のレースを確認します…', '#e2b96f'); await tick(true); } const mobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent); if (mobile) { log('スマホでは定期監視は動きません（タブが止まるため）。' + 'レースごとにリンクを開いて実行してください。', '#ffb74d'); } else { log(`監視開始。${CFG.RACE_HOURS.join('/')}時の発走${CFG.LEAD_SEC}秒前に解析します。`, '#e2b96f'); window.__oasisAutopilotTimer = setInterval(() => tick(false), 20000); } } catch (e) { log('起動できません: ' + esc(e.message), '#ef5350'); } })();
})();
