# モデル・データ レビュー（2026/09/01）

対象: `model.json`（CORE 3.19.0 / 学習 158レース / 2026-07-28〜08-24）と
`races.jsonl`（自動採取 407レース / 2026-06-25〜09-01）。

---

## 0. 結論だけ

| # | 見つけたこと | 対応 |
|---|---|---|
| 1 | ビルドは完全に再現する | 対応不要 |
| 2 | **学習データが8日分止まっている**（logg は 08/24 まで、採取は 09/01 まで） | Discord を再エクスポートして再学習 |
| 3 | 装備ボーナスの欠落は **08/26 に直っている**。08/25以前の 197頭（装備持ちの33%）は今も低いまま | races.jsonl を使う解析は `race_date >= 2026-08-26` で切る |
| 4 | 学習期間外の精度は落ちていない（スピアマン 0.948 / 1着 77.8%） | 対応不要 |
| 5 | 3連単の本命1点が予測を下回っている（予測44.9% vs 実測31.2%） | σは触らず監視。20レース貯めて再確認 |

---

## 1. ビルドの再現性 ✅

`logg/` から `train_model` を回し直したところ、配布中の `model.json` と一致しました。

```
学習完了  1864行 / 158レース （2026/07/28〜2026/08/24, mode=new_only）
精度(OOF) レース内スピアマン=0.946  1着的中=87%  残差std=0.0800  α=16.0
RACE_SIGMA（単勝）=0.0127   TRI_SIGMA（3連単）=0.0137
```

係数37個・σ・スピアマンすべて `model.json` と同値。ビルド経路に壊れはありません。

---

## 2. 学習データが8日分止まっている ⚠ 最優先

`logg/race.txt` の最新は **2026-08-24**。一方 `harvest_daily.bat` は毎日回っていて
`races.jsonl` は **09-01** まで入っています。つまり直近8日・約48レースが学習に入っていません。

学習は `logg/` のテキストしか読まないので、採取が進んでもモデルは古いままです。
Discord の「🏇｜おあしすっちレース情報」チャンネルを再エクスポートして `logg/` に置き、
`python build_autopilot.py` を回すのが一番効きます。

参考に、学習データ内の1着的中を週ごとに割ると単調に下がっています（in-sample）:

| 週 | レース | スピアマン | 1着的中 |
|---|---|---|---|
| 07/27〜08/02 | 36 | 0.957 | 91.7% |
| 08/03〜08/09 | 41 | 0.925 | 97.6% |
| 08/10〜08/16 | 40 | 0.944 | 85.0% |
| 08/17〜08/23 | 40 | 0.959 | 77.5% |

順位相関は落ちていないので「予測が壊れた」のではなく、**1着の取り合いが接戦になっている**
＝環境が整って上位の差が詰まってきた、という読みが自然です。新しいデータを入れる価値はあります。

---

## 3. 装備ボーナスの欠落は 08/26 に直っている

`logg`（Discordの当時値）と `races.jsonl` を同一レースで突き合わせました（232頭）。

| | 頭数 | speed の一致率 | 平均差（logg − jsonl） |
|---|---|---|---|
| 装備なし | 163 | **96.9%** | −0.13 |
| 装備あり | 69 | **42.0%** | **+3.91** |
| お守りあり | 59 | 44.1% | **+4.28** |

ズレているのは装備・お守り持ちだけで、`races.jsonl` 側が**低い**。
`merge_horse` の `item_bonus` 加算が入る前に採取された行です。

日付ごとに「`speed == base + train + item_bonus` になっているか」を数えると、直った日がはっきり出ます:

```
08/24  n=41  加算済み 11  未加算 30
08/25  n=50  加算済み 34  未加算 16     ← 修正が入った日
08/26  n=44  加算済み 44  未加算  0
…
09/01  n=14  加算済み 14  未加算  0
全体   n=590 加算済み 393 (66.6%)  未加算 197 (33.4%)
```

**影響**: `evaluate.py` / `fit_ridge_stamina.py` / スタミナ法則の実測など、`races.jsonl` の
ステータスを使う解析はすべて、装備持ち約1/3が過小評価された状態で回っていました。
係数を実測し直すときは `race_date >= 2026-08-26` に絞ってください。

**`--refresh` は万能ではありません**。`item_bonus` は by-id（＝今の装備）から取るので、
古いレースを refresh すると「当時つけていなかった装備」を足してしまいます。
`harvest_results.py` の冒頭コメントが言う通り、**ステータスが要る解析は `logg` が正**です。

---

## 4. 学習期間外での実測 ✅

装備ボーナスが正しく入っていて、かつ学習に使われていない **08/26〜09/01 の36レース**で
配布中のモデルを採点しました。

| | スピアマン | 1着的中 |
|---|---|---|
| 学習時のCV（参考） | 0.946 | 87% |
| 08/26〜09/01（完全に未知） | **0.948** | **77.8%** |

順位相関は学習時と同じ。1着的中の 87%→78% は36レースでは誤差の範囲（±7%）です。
**モデルは劣化していません。**

---

## 5. 校正: 単勝は合っている / 3連単は下振れ

同じ36レースで「予測確率 vs 実際に当たった率」を見ました。

| | 予測 | 実測 | 判定 |
|---|---|---|---|
| 単勝 本命 | 81.4% | 77.8% | ✅ 合っている |
| 3連単 本命1点（8頭以上32R） | 44.9% | 31.2% (10/32) | ⚠ 下振れ |

3連単だけ、σ を1.5倍にすると数字が合います（予測29.5% vs 実測28.1%）。
ただし **32レースでは 1.6σ 程度**で、偶然の範囲を出ていません。
モデル自身の自己校正（学習データ80レースのOOF）はむしろ逆に
「予測44% vs 実測51%」と控えめ側に出ています。

σ を手で膨らませないのはこのツールの設計方針であり、ここでも守るべきだと考えます。
資金の保険は分数ケリーとλが担う、という現行の役割分担のままで問題ありません。
**ただし、3連単のEVは実測より1.4倍ほど高く出ている可能性があります。**
20レース分の実結果が貯まったら `fit_model_weight.py` で λ を測り直すのが正攻法です。

---

## 6. 次にやるなら（優先順）

1. **Discord を再エクスポートして再学習** — 8日分の穴を埋める。効果が一番大きい
2. `races.jsonl` を使う解析の対象を `>= 2026-08-26` に統一（装備ボーナス込みの行だけ）
3. 実購入が20件貯まったら λ の実測（3連単の下振れが本物かどうかはこれで決まる）
