# レースエンジンの逆算メモ（2026/09/01）

`races.jsonl` の timeline 39,670区間 / 2,394頭から、ゲーム内部の計算を割り出したもの。
**現行モデル（Ridge で最終スコアを直あて）とは別の道**であり、まだ実装していない。

---

## 1. 区間速度の法則

```
v(区間) = k(レース) × rating^0.8709 × fatigue^0.9136        [m/s]
```

`rating` と `fatigue_modifier` は timeline にそのまま入っている値。

検証（レース×フェーズで中心化した log(v/rating) の残差）:

| 説明変数 | 残差sd |
|---|---|
| なし | 6.68% |
| **+ log(fatigue)** | **1.24%** |
| + 残スタミナ率も | 1.24% |
| + 順位・発動パッシブ数も | 1.24% |

同じ馬・同じフェーズ内のばらつきが 1.72%、ゲーム自身の乱数が 1.19%（後述）なので、
**1.24% は乱数の下限とほぼ同じ**。この式で区間速度は説明し切れている。

`k(レース)` はレースごとの定数（馬場状態と思われる）。順位にはレース内で消えるので不要。

## 2. rating

**フェーズ内で完全に一定**（6,855組中 95.5%）。乱数ではなく計算値。

```
rating = a·実効SP + b·実効PW + c·実効ST + d        （距離×フェーズごと）
```

レース単位5分割CVで **検証残差 0.95%**（装備なし1,611行）。

- **同族嫌悪の反映漏れが最大の誤差源**だった（入れるだけで 3.24% → 1.29%）
- **コンディションは rating に入らない**（不調 −0.26% / 好調 −0.12%、差なし）
- 上限・逓減なし。実効SP 259 でも線形（適用 ×1.6875 に対し逆算 ×1.6939）

割り出した重み（正規化）と現行の内部定数の比較:

| 距離 | フェーズ | 実測 SP/PW/ST | 現行 SP/PW/ST | ずれ |
|---|---|---|---|---|
| 短距離 | 序盤 | 0.738 / 0.209 / 0.053 | 0.743 / 0.212 / 0.044 | 0.009 |
| 短距離 | 中盤 | 0.650 / 0.167 / 0.184 | 0.653 / 0.166 / 0.181 | 0.003 |
| 短距離 | 終盤 | 0.532 / 0.305 / 0.162 | 0.533 / 0.304 / 0.163 | 0.001 |
| マイル | 序盤 | 0.617 / 0.307 / 0.076 | 0.600 / 0.300 / 0.100 | 0.024 |
| **マイル** | **中盤** | **0.509 / 0.227 / 0.263** | 0.450 / 0.200 / 0.350 | **0.087** |
| **マイル** | **終盤** | **0.389 / 0.392 / 0.219** | 0.350 / 0.350 / 0.300 | **0.081** |
| 中距離 | 序盤 | 0.515 / 0.374 / 0.112 | 0.509 / 0.368 / 0.123 | 0.011 |
| 中距離 | 中盤 | 0.367 / 0.239 / 0.394 | 0.362 / 0.232 / 0.406 | 0.012 |
| 中距離 | 終盤 | 0.274 / 0.402 / 0.324 | 0.272 / 0.392 / 0.336 | 0.013 |
| 長距離 | 序盤 | 0.438 / 0.361 / 0.200 | 0.450 / 0.375 / 0.175 | 0.025 |
| 長距離 | 中盤 | 0.262 / 0.192 / 0.546 | 0.281 / 0.208 / 0.510 | 0.035 |
| 長距離 | 終盤 | 0.203 / 0.333 / 0.464 | 0.214 / 0.357 / 0.429 | 0.035 |

**マイルの中盤・終盤だけスタミナを重く見すぎている。** 他は誤差の範囲。

## 3. スタミナと fatigue

- `stamina_cost` も**フェーズ内で一定**。残スタミナはその分ずつ線形に減る
- `fatigue_modifier` は残スタミナがある間は **≒1.005**（残り率にほとんど依存しない）
- **スタミナが尽きた瞬間から 0.65 で固定**（移行区間だけ 0.73）

| 残スタミナ率 | fatigue 中央値 |
|---|---|
| 0（枯渇） | **0.6500** |
| 0〜5% | 0.9342 |
| 5〜10% | 1.0025 |
| 10%以上 | 1.005 前後で一定 |

枯渇後の区間ごと: 0.7295（1区間目）→ 以降ずっと 0.6500。

**0.65 という値は core が既に持っている定数と同じ**（selftest P10「スタミナ切れの罰は 0.65 で頭打ち」）。

## 4. ゲーム側の乱数

rating を固定した同じ馬・同じフェーズで区間速度の相対sd = **1.19%**
（運営告知の「乱数幅1.5%」に対応。2026/08/04 に 1.5→3%、08/17 に 3→1.5% へ戻された）。

区間ごとに独立なので、**合計タイムに残るのは 1.19% / √16 = 0.30%**。これが理論上の下限。

## 5. 通しで動かした結果

### 実測値から組み立てる（エンジンが解けているかの確認）

```
T = Σ 1 / (rating^0.8709 × fatigue^0.9136)
```

実タイムとの相関 **0.9997** / 残差 **0.64%** / スピアマン 0.9948 / 1着的中 **97.8%**（2,278頭）。
→ **rating と fatigue さえ分かれば着順はほぼ決まる。**

### レース前情報だけで予測して動かす（543頭・装備なし・OOF）

| | スピアマン | 1着的中 | タイム残差 |
|---|---|---|---|
| 実測 rating/cost を使用（上限） | 0.9709 | 88.5% | 2.67% |
| **予測 rating/cost（レース前のみ）** | **0.9643** | 78.7% | **3.22%** |
| 現行モデル（同じ馬集合） | 0.9198 | 80.3% | 13.91% |

**スピアマンは 0.920 → 0.964、タイム残差は 13.9% → 3.2% に改善。**
1着的中だけは 80.3% → 78.7% と微減（96レースでは誤差の範囲）。

## 6. いまのボトルネック

レース前情報からの予測精度:

| 予測対象 | 検証誤差 |
|---|---|
| rating 序盤 / 中盤 / 終盤 | 1.49% / 1.22% / 1.21% |
| **stamina_cost 序盤 / 中盤 / 終盤** | **7.71% / 6.89% / 6.86%** |

**stamina_cost の予測が桁違いに悪い。** しかも cost の誤差は「スタミナが尽きるか否か」
＝ fatigue 1.005 か 0.65 かの崖を左右するので、影響が非線形に大きい。
上限（実測値使用）との差 88.5% → 78.7% はほぼこれが原因。

なお core には既に `STAMINA_COST_LAW` があり、そちらのほうが良い可能性がある（未検証）。

## 7. 次にやるなら

1. **stamina_cost の予測を詰める**（ここが全て。`STAMINA_COST_LAW` との比較から）
2. マイル中盤・終盤の内部定数を実測値に寄せる
3. rating を使った採取データの健全性チェック
   （記録上のステータス・パッシブから計算した rating と実測 rating が3%以上ずれる馬は、
    採取時点と当時が食い違っている。今回 ちゃん・ぬまるん の18行が該当）

移行は大手術（core の予測経路・JS移植・パリティ・再学習）なので、
まず 1 を詰めて上限に届くか確かめてから判断する。


---

# 追記: stamina_cost を詰めた（2026/09/01）

## clamp は timeline から直読みできる

**消費を変えるパッシブが1つも発動していない区間だけ**を集めると、消費の最小値・最大値に
数千件がぴったり張り付く。それが clamp の両端そのもの。

| 距離 | 実測 lo（件数） | 実測 hi（件数） | 旧 `STAMINA_COST_LAW` |
|---|---|---|---|
| 短距離 | **2.125**（1,082件） | **2.875**（1,506件） | 2.125 / 2.879 |
| マイル | **2.267**（1,976件） | **3.067**（2,241件） | 2.234 / 3.067 |
| 中距離 | **2.762**（1,272件） | **3.737**（1,296件） | 2.541 / 3.737 |
| 長距離 | **2.720**（1,154件） | **3.680**（734件） | 2.570 / 3.680 |

中距離・長距離の lo が特にずれていた（旧値には**該当0件**）。修正済み（core 3.20.0）。
194レースで 1着的中 82.0% → 82.5%、残差std 0.0795 → 0.0785、σ 0.0217 → 0.0215。

## 消費の倍率は clamp の**後**に掛かる

clamp に張り付いた点で `cost / clamp値` を読むと、きれいな倍率が出る。
しかも**その区間でパッシブが発動しているときだけ**掛かる（区間ごとに変わるのはこのため）。

| パッシブ | 倍率 | 根拠 |
|---|---|---|
| 省エネ走法 `energy_saver` | **×0.92** | 4距離すべてで一致（0.9197〜0.9200） |
| ロングスパート `long_spurt` | **×1.08** | 4距離すべてで一致（1.0798〜1.0801） |
| 逃げ `front_runner` | ×1.12 | マイル・短距離 |
| ペース配分 `pace_control` | ×0.85 | マイル |
| **首位の呪い** `gear_leader_curse` | **×1.04** | 短距離 1.047 / 長距離 1.032 |
| お守り省エネ `charm_energy_saver` | ×0.98 | マイル |
| 粘り腰 `tenacious` | ×1.11 | マイル |

**首位の呪いの「スタミナ消費も増加」は ×1.04** と読めた（区間消費の実測 ×1.049 とも整合）。

## モデルには入れていない

倍率を発動割合で重み付けして `stamina_budget` の必要量に掛ける実装も試したが、
**効果ゼロ**だった（スピアマン 0.9346 → 0.9347、1着的中は変わらず）。入れていない。

## 残っている誤差

倍率を割り戻したうえで stats から cost を解いても、誤差は距離により 3.0〜7.5%。

| 距離 | 旧法則 | 今回 |
|---|---|---|
| 長距離 | 10.22% | **2.95%** |
| 中距離 | 8.49% | **4.52%** |
| 短距離 | 7.02% | 6.05% |
| マイル | 8.83% | 7.52% |

倍率を割り戻した後も1頭のレース中で cost が一定になるのは35%だけなので、
**まだ見つけていない倍率パッシブが残っている**。マイル・短距離が悪いのは、
clamp に張り付いていない点が半分以下しかなく、係数を決める材料が足りないため。

シミュレータに移行するなら、ここを 1% 台まで詰めるのが条件（1着的中の上限 88.5% と
実装値 78.7% の差はほぼ cost の誤差）。
