# レースエンジンの逆算メモ（2026/09/01）

`races.jsonl` の timeline 39,670区間 / 2,394頭から、ゲーム内部の計算を割り出したもの。
**現行モデル（Ridge で最終スコアを直あて）とは別の道**であり、まだ実装していない。

---

## 1. 区間速度の法則

```
v(区間) = k(レース) × rating^0.8709 × fatigue^0.9136        [m/s]
```

`rating` と `fatigue_modifier` は timeline にそのまま入っている値。

検証（レース×フェーズで中心化した log(v/rating) の残差）:

| 説明変数 | 残差sd |
|---|---|
| なし | 6.68% |
| **+ log(fatigue)** | **1.24%** |
| + 残スタミナ率も | 1.24% |
| + 順位・発動パッシブ数も | 1.24% |

同じ馬・同じフェーズ内のばらつきが 1.72%、ゲーム自身の乱数が 1.19%（後述）なので、
**1.24% は乱数の下限とほぼ同じ**。この式で区間速度は説明し切れている。

`k(レース)` はレースごとの定数（馬場状態と思われる）。順位にはレース内で消えるので不要。

## 2. rating

**フェーズ内で完全に一定**（6,855組中 95.5%）。乱数ではなく計算値。

```
rating = a·実効SP + b·実効PW + c·実効ST + d        （距離×フェーズごと）
```

レース単位5分割CVで **検証残差 0.95%**（装備なし1,611行）。

- **同族嫌悪の反映漏れが最大の誤差源**だった（入れるだけで 3.24% → 1.29%）
- **コンディションは rating に入らない**（不調 −0.26% / 好調 −0.12%、差なし）
- 上限・逓減なし。実効SP 259 でも線形（適用 ×1.6875 に対し逆算 ×1.6939）

割り出した重み（正規化）と現行の内部定数の比較:

| 距離 | フェーズ | 実測 SP/PW/ST | 現行 SP/PW/ST | ずれ |
|---|---|---|---|---|
| 短距離 | 序盤 | 0.738 / 0.209 / 0.053 | 0.743 / 0.212 / 0.044 | 0.009 |
| 短距離 | 中盤 | 0.650 / 0.167 / 0.184 | 0.653 / 0.166 / 0.181 | 0.003 |
| 短距離 | 終盤 | 0.532 / 0.305 / 0.162 | 0.533 / 0.304 / 0.163 | 0.001 |
| マイル | 序盤 | 0.617 / 0.307 / 0.076 | 0.600 / 0.300 / 0.100 | 0.024 |
| **マイル** | **中盤** | **0.509 / 0.227 / 0.263** | 0.450 / 0.200 / 0.350 | **0.087** |
| **マイル** | **終盤** | **0.389 / 0.392 / 0.219** | 0.350 / 0.350 / 0.300 | **0.081** |
| 中距離 | 序盤 | 0.515 / 0.374 / 0.112 | 0.509 / 0.368 / 0.123 | 0.011 |
| 中距離 | 中盤 | 0.367 / 0.239 / 0.394 | 0.362 / 0.232 / 0.406 | 0.012 |
| 中距離 | 終盤 | 0.274 / 0.402 / 0.324 | 0.272 / 0.392 / 0.336 | 0.013 |
| 長距離 | 序盤 | 0.438 / 0.361 / 0.200 | 0.450 / 0.375 / 0.175 | 0.025 |
| 長距離 | 中盤 | 0.262 / 0.192 / 0.546 | 0.281 / 0.208 / 0.510 | 0.035 |
| 長距離 | 終盤 | 0.203 / 0.333 / 0.464 | 0.214 / 0.357 / 0.429 | 0.035 |

**マイルの中盤・終盤だけスタミナを重く見すぎている。** 他は誤差の範囲。

## 3. スタミナと fatigue

- `stamina_cost` も**フェーズ内で一定**。残スタミナはその分ずつ線形に減る
- `fatigue_modifier` は残スタミナがある間は **≒1.005**（残り率にほとんど依存しない）
- **スタミナが尽きた瞬間から 0.65 で固定**（移行区間だけ 0.73）

| 残スタミナ率 | fatigue 中央値 |
|---|---|
| 0（枯渇） | **0.6500** |
| 0〜5% | 0.9342 |
| 5〜10% | 1.0025 |
| 10%以上 | 1.005 前後で一定 |

枯渇後の区間ごと: 0.7295（1区間目）→ 以降ずっと 0.6500。

**0.65 という値は core が既に持っている定数と同じ**（selftest P10「スタミナ切れの罰は 0.65 で頭打ち」）。

## 4. ゲーム側の乱数

rating を固定した同じ馬・同じフェーズで区間速度の相対sd = **1.19%**
（運営告知の「乱数幅1.5%」に対応。2026/08/04 に 1.5→3%、08/17 に 3→1.5% へ戻された）。

区間ごとに独立なので、**合計タイムに残るのは 1.19% / √16 = 0.30%**。これが理論上の下限。

## 5. 通しで動かした結果

### 実測値から組み立てる（エンジンが解けているかの確認）

```
T = Σ 1 / (rating^0.8709 × fatigue^0.9136)
```

実タイムとの相関 **0.9997** / 残差 **0.64%** / スピアマン 0.9948 / 1着的中 **97.8%**（2,278頭）。
→ **rating と fatigue さえ分かれば着順はほぼ決まる。**

### レース前情報だけで予測して動かす（543頭・装備なし・OOF）

| | スピアマン | 1着的中 | タイム残差 |
|---|---|---|---|
| 実測 rating/cost を使用（上限） | 0.9709 | 88.5% | 2.67% |
| **予測 rating/cost（レース前のみ）** | **0.9643** | 78.7% | **3.22%** |
| 現行モデル（同じ馬集合） | 0.9198 | 80.3% | 13.91% |

**スピアマンは 0.920 → 0.964、タイム残差は 13.9% → 3.2% に改善。**
1着的中だけは 80.3% → 78.7% と微減（96レースでは誤差の範囲）。

## 6. いまのボトルネック

レース前情報からの予測精度:

| 予測対象 | 検証誤差 |
|---|---|
| rating 序盤 / 中盤 / 終盤 | 1.49% / 1.22% / 1.21% |
| **stamina_cost 序盤 / 中盤 / 終盤** | **7.71% / 6.89% / 6.86%** |

**stamina_cost の予測が桁違いに悪い。** しかも cost の誤差は「スタミナが尽きるか否か」
＝ fatigue 1.005 か 0.65 かの崖を左右するので、影響が非線形に大きい。
上限（実測値使用）との差 88.5% → 78.7% はほぼこれが原因。

なお core には既に `STAMINA_COST_LAW` があり、そちらのほうが良い可能性がある（未検証）。

## 7. 次にやるなら

1. **stamina_cost の予測を詰める**（ここが全て。`STAMINA_COST_LAW` との比較から）
2. マイル中盤・終盤の内部定数を実測値に寄せる
3. rating を使った採取データの健全性チェック
   （記録上のステータス・パッシブから計算した rating と実測 rating が3%以上ずれる馬は、
    採取時点と当時が食い違っている。今回 ちゃん・ぬまるん の18行が該当）

移行は大手術（core の予測経路・JS移植・パリティ・再学習）なので、
まず 1 を詰めて上限に届くか確かめてから判断する。


---

# 追記: stamina_cost を詰めた（2026/09/01）

## clamp は timeline から直読みできる

**消費を変えるパッシブが1つも発動していない区間だけ**を集めると、消費の最小値・最大値に
数千件がぴったり張り付く。それが clamp の両端そのもの。

| 距離 | 実測 lo（件数） | 実測 hi（件数） | 旧 `STAMINA_COST_LAW` |
|---|---|---|---|
| 短距離 | **2.125**（1,082件） | **2.875**（1,506件） | 2.125 / 2.879 |
| マイル | **2.267**（1,976件） | **3.067**（2,241件） | 2.234 / 3.067 |
| 中距離 | **2.762**（1,272件） | **3.737**（1,296件） | 2.541 / 3.737 |
| 長距離 | **2.720**（1,154件） | **3.680**（734件） | 2.570 / 3.680 |

中距離・長距離の lo が特にずれていた（旧値には**該当0件**）。修正済み（core 3.20.0）。
194レースで 1着的中 82.0% → 82.5%、残差std 0.0795 → 0.0785、σ 0.0217 → 0.0215。

## 消費の倍率は clamp の**後**に掛かる

clamp に張り付いた点で `cost / clamp値` を読むと、きれいな倍率が出る。
しかも**その区間でパッシブが発動しているときだけ**掛かる（区間ごとに変わるのはこのため）。

| パッシブ | 倍率 | 根拠 |
|---|---|---|
| 省エネ走法 `energy_saver` | **×0.92** | 4距離すべてで一致（0.9197〜0.9200） |
| ロングスパート `long_spurt` | **×1.08** | 4距離すべてで一致（1.0798〜1.0801） |
| 逃げ `front_runner` | ×1.12 | マイル・短距離 |
| ペース配分 `pace_control` | ×0.85 | マイル |
| **首位の呪い** `gear_leader_curse` | **×1.04** | 短距離 1.047 / 長距離 1.032 |
| お守り省エネ `charm_energy_saver` | ×0.98 | マイル |
| 粘り腰 `tenacious` | ×1.11 | マイル |

**首位の呪いの「スタミナ消費も増加」は ×1.04** と読めた（区間消費の実測 ×1.049 とも整合）。

## モデルには入れていない

倍率を発動割合で重み付けして `stamina_budget` の必要量に掛ける実装も試したが、
**効果ゼロ**だった（スピアマン 0.9346 → 0.9347、1着的中は変わらず）。入れていない。

## 残っている誤差

倍率を割り戻したうえで stats から cost を解いても、誤差は距離により 3.0〜7.5%。

| 距離 | 旧法則 | 今回 |
|---|---|---|
| 長距離 | 10.22% | **2.95%** |
| 中距離 | 8.49% | **4.52%** |
| 短距離 | 7.02% | 6.05% |
| マイル | 8.83% | 7.52% |

倍率を割り戻した後も1頭のレース中で cost が一定になるのは35%だけなので、
**まだ見つけていない倍率パッシブが残っている**。マイル・短距離が悪いのは、
clamp に張り付いていない点が半分以下しかなく、係数を決める材料が足りないため。

シミュレータに移行するなら、ここを 1% 台まで詰めるのが条件（1着的中の上限 88.5% と
実装値 78.7% の差はほぼ cost の誤差）。


---

# 追記2: stamina_cost は「ステータスの式」では出せない（2026/09/01・決着）

## 隠れパッシブではなかった

1頭のレース中で cost が変わる箇所 5,598件を、何が同時に変わったかで分類:

| 同時に変わったもの | 割合 |
|---|---|
| rating | 63.6% |
| フェーズ | 59.0% |
| 発動パッシブ | 13.5% |
| どれも変わっていない | 36.3% |

「どれも変わっていない」2,034件の比は 0.9997 / 1.0004 / 1.0021 …と **0.1〜0.3%の丸め誤差**。
未発見の倍率パッシブではない。

## しかしステータスからは決まらない

**同じ馬・同じステータス・同じ距離・同じフェーズ・同じ rating・同じコンディション**の
組み合わせを 306件集めたところ、**290件（94.8%）で cost が割れていた。**

例（いちご SP157 PW47 ST50 / マイル / 序盤 / コンディション普通）:

```
R1861  cost=2.789      R1896  cost=3.067
R1945  cost=2.731      R1968  cost=3.067
R1981  cost=2.646      R1993  cost=2.728
```

**同一条件で 2.646〜3.067（±16%）。** レースごとに変わる何かに依存している。
上限 3.067 に張り付く回とそうでない回があるので、clamp 前の値が大きく動いている。

## 試して外れた形（すべて 5〜10% で頭打ち）

| 仮説 | 誤差sd |
|---|---|
| clamp(k[距離] × rating) | 6.5〜10.7% |
| clamp(a×rating + b) | 6.7〜 9.9% |
| clamp(k[距離,フェーズ] × rating) | 4.5〜 7.5% |
| clamp(実効SP/PW/ST の線形和) | 5.4〜 8.8% |
| clamp(素のSP/PW/ST の線形和) | 7.1〜 9.8% |
| ＋ コンディション / 馬場 / 発動パッシブ数 | 改善なし |

`ground`（馬場状態）は races.jsonl では空。`surface`（芝/ダート）は無関係（z平均 ±0.015）。

## 副産物: 初期スタミナは計算できる

`timeline[0].stamina` = **floor(実効ST)** が 82.6%で一致（1,163頭）。
残りは +1/+2/+5 など小さい整数のずれで、装備の加算ぶんと採取のズレ。
core の `stamina_budget` は既に `floor(eff['stamina'])` を使っており、ここは正しい。

## 意味すること

- **レース前情報だけから cost を出す道は閉じている。** 残っているのは
  「レースごとに変わる何か」。（※ 当初 出走メンバー・ペース・位置取り と書いたが誤り。追記3参照）
- したがって前回測った**シミュレータの上限 1着的中 88.5%（実測 cost を使用）は
  レース前には到達できない**。実装値 78.7% との差の大半は、埋められない差だった。
- 現行モデルはこの不確かさを σ に吸収している。設計として妥当。

**シミュレータへの移行は保留。** 再開する条件は「cost を動かしている
レースごとの変数を timeline から特定できること」。位置取り（start_position / start_rank）と
cost の関係を、同一馬・同一ステータスの組で追うのが次の一手。


---

# 追記3: cost のばらつきはレースごとの乱数だった（2026/09/01・決着）

「出走メンバー・ペース・位置取り」は関係ない（ゲーム仕様上、出走メンバーが関わるのは
同族嫌悪パッシブのみ）。残りの候補を1つずつ潰した。

## 潰した候補

| 候補 | 結果 |
|---|---|
| 未発見の倍率パッシブ | ✗ 説明のつかない変化は比 0.9997〜1.0021 の丸め誤差だけ |
| ステータスの乱数（1.5%/3%） | ✗ `rating` のレース間 cv は **0.000%**（rating は振れない）。cost の cv も 乱数幅 1.5%→3%→1.5% の切替に追随しない（4.30% → 3.33% → 4.83%） |
| コンディション | ✗ 鍵に入れてもばらつきは変わらない（5.55%） |
| 馬場（芝/ダート） | ✗ z平均 ±0.015 |
| 出走頭数・第何レースか | △ 23時（上位レース・6頭以下）だけ −7.8%。ただし両者は完全に交絡していて分離不能。**除外してもなお 4.70% 残る** |
| 装備・お守りの記録漏れ | ✗ **装備実装前（〜08/17）でも 3.40% ばらつく** |

## 残ったもの

同じ馬・同じステータス・同じパッシブ・同じコンディション・同じ距離・同じフェーズで、
レースをまたぐと cost が **3〜5%（最大30%）** 動く。序盤・中盤・終盤は**まとまって動く**ので、
1レース1頭につき1つのスカラーが掛かっている形。

実例（テト SP148 PW42 ST49 / 短距離 / 好調 / 装備なし）:

```
R2004 21:00 15頭  序盤 2.826  中盤 2.831
R2122 15:00 13頭  序盤 2.612  中盤 2.612
R2170 15:00 12頭  序盤 2.155  中盤 2.195
R2196 21:00  9頭  序盤 2.334  中盤 2.361
```

装備が存在しなかった時期にも同じ現象が起きているので、**レースごとに引かれる乱数**と考えるのが
自然。運営告知の「ステータスの乱数幅」とは別物（そちらは rating に乗っておらず、切替にも追随しない）。

## 結論

**cost の予測誤差 5〜8% は解析の失敗ではなく、予測可能性の上限だった。**
どんな式を立ててもここから下には行かない。現行モデルがこれを σ に吸収している設計は正しい。

シミュレータの上限（実測 cost を使った 1着的中 88.5%）は**原理的に到達不能**。
レース前に到達できるのは 78.7% 付近で、現行モデルの 82.5% を下回る。
**シミュレータへの移行は行わない。**

ただし追記1・追記2で得たものは残る:
- 区間速度の法則 v = k×rating^0.87×fatigue^0.91（レース内残差 1.24%）
- rating の式（検証誤差 0.95%）と、内部定数がマイル中盤・終盤でずれていること
- スタミナ消費 clamp の実測値（core 3.20.0 で修正済み）
- 消費の倍率（省エネ走法 ×0.92 / 首位の呪い ×1.04 など、clamp の後に掛かる）
- 初期スタミナ = floor(実効ST)（82.6%一致・core は既に正しい）


---

# 追記4: 内部定数を実測に寄せてみた結果（2026/09/01）

## 重みは「フェーズ × 距離」に分解できる

実測した12組（距離×フェーズ）の SP/PW/ST 比を、現行と同じ
`INTERNAL_PHASE_WEIGHTS[フェーズ] × INTERNAL_DIST_BALANCE[距離]` の形（rank-1）に
交互最小二乗で分解したところ、**きれいに乗った**。

| | 平均ずれ | 最大ずれ |
|---|---|---|
| 現行の定数 | 0.0280 | 0.0868（マイル中盤） |
| 実測に寄せた定数 | **0.0053** | **0.0096** |

つまり**ゲームの rating の重みは本当にフェーズと距離の積で書けている**。両方の表を復元できた。

```python
INTERNAL_PHASE_WEIGHTS = {          # 実測
    '序盤': [0.6183, 0.3077, 0.0739],
    '中盤': [0.5073, 0.2269, 0.2658],
    '終盤': [0.3883, 0.3896, 0.2221],
}
INTERNAL_DIST_BALANCE = {           # 実測（マイルを [1,1,1] に正規化）
    '短距離': [1.3737, 0.7838, 0.7669],
    'マイル': [1.0000, 1.0000, 1.0000],
    '中距離': [0.7057, 1.0300, 1.3926],
    '長距離': [0.5054, 0.8312, 1.9867],
}
# 重みを変えると base の絶対値が変わるので c も引き直しが要る
STAMINA_COST_LAW の c: マイル 0.018597 / 中距離 0.036975 / 短距離 0.012022 / 長距離 0.043416
```

## しかし採用しない（実測して悪化した）

194レースの A/B:

| | スピアマン | 1着的中 | 残差std | σ |
|---|---|---|---|---|
| 現行の定数 | **0.9346** | **82.5%** | 0.0785 | 0.0215/0.0254 |
| 実測に寄せた定数 | 0.9335 | 80.9% | 0.0780 | 0.0215/0.0272 |

**理由**: この2つの定数は core では `rating` の計算には**使われていない**。
Ridge の特徴量は距離ごとの log項・線形項であって、フェーズ重みは通らない。
実際に使われているのは `stamina_budget` の `base`（＝必要スタミナの見積もり）と、
アプリの表示用 `internal_stat_weights` だけ。

現行の値は**スタミナ収支に合うように経験的に調整されたもの**で、
rating の重みとは別の量。rating 用の正しい値に差し替えると、
本来の用途であるスタミナ収支の見積もりが崩れて 1着的中が 1.6ポイント落ちる。

cost の予測誤差も、長距離が 9.02% → 7.65% に改善する以外はほぼ変わらない
（追記3のとおり cost はレースごとの乱数が支配的なため）。

**結論: 定数は現行のまま。** 実測値は上に記録として残す。
rating を直接使う設計（シミュレータ）に移るときに初めて必要になる値。
